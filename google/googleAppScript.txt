// ===== video_summary =====
公式サイトを確認したのちに、新しいスプレッドシートに紐付いたプロジェクトを作成していきます。


    公式サイトの確認
    MySpreadsheetの作成
    MyProjectの作成

// ===== commentBox 1 =====
補足情報
必要となる知識について




2018年3月20日





今回のレッスンについては以下も参考にしてみてください。

・JavaScript入門 (全24回)
https://dotinstall.com/lessons/basic_javascript_v2

// ===== コード.gs =====
function myFunction() {

}





// ===== video_summary =====
Loggerを使って、ログ画面に値を表示させる方法について見ていきます。


    GASで使える命令の注意点
    Logger
    ログ画面の表示

// ===== コード.gs =====
function myFunction() {
//  alert('hello');
  Logger.log('Hello from Logger!');
}






// ===== video_summary =====
Spreadsheetの機能を簡単に拡張できるカスタム関数を作っていきます。


    カスタム関数の作成と保存
    動作確認

// ===== コード.gs =====
function GETRESULT(input) {
  if (input >= 80) {
    return 'PASS';
  } else {
    return 'FAIL';
  }
}





// ===== video_summary =====
カスタム関数において複数のセルを扱う方法について見ていきます。


    複数セルの処理

// ===== コード.gs =====
function GETRESULT(input) {
  if (input.map) {
    return input.map(GETRESULT);
  } else {
    return input >= 80 ? 'PASS' : 'FAIL';
  }
//  if (input >= 80) {
//    return 'PASS';
//  } else {
//    return 'FAIL';
//  }
}





// ===== video_summary =====
スプレッドシートの値をスクリプトから操作する方法について見ていきます。


    initSheet()の作成
    シートの初期化処理
    Permissionの許可

// ===== コード.gs =====
function initSheet() {
  var sheet = SpreadsheetApp.getActiveSheet();

  sheet.clear();

  sheet.getRange(1, 1).setValue('taguchi');
  sheet.getRange(1, 2).setValue(82).setBackground('tomato');
}






// ===== video_summary =====
ループ処理を使ってランダムなデータをいっきに追加する方法について見ていきます。


    複数データの一括追加
    乱数の生成

// ===== コード.gs =====
function initSheet() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var names = ['taguchi', 'fkoji', 'dotinstall'];
  var i;

  sheet.clear();

  for (i = 1; i <= 100; i++) {
    sheet.getRange(i, 1).setValue(names[Math.floor(Math.random() * names.length)]);
    sheet.getRange(i, 2).setValue(Math.floor(Math.random() * 101));
  }
}





// ===== video_summary =====
データの処理にかかる時間を計測する方法について見ていきます。


    処理時間の計測
    ログの確認

// ===== コード.gs =====
function initSheet() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var names = ['taguchi', 'fkoji', 'dotinstall'];
  var i;
  var startTime = new Date();

  sheet.clear();

//  2060ms
for (i = 1; i <= 1000; i++) {
  sheet.getRange(i, 1).setValue(names[Math.floor(Math.random() * names.length)]);
  sheet.getRange(i, 2).setValue(Math.floor(Math.random() * 101));
}

  Logger.log(new Date() - startTime);
}





// ===== video_summary =====
大量のデータを高速で処理する方法について見ていきます。


    データ処理の高速化
    ログの確認

// ===== コード.gs =====
function initSheet() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var names = ['taguchi', 'fkoji', 'dotinstall'];
  var i;
  var startTime = new Date();
  var scores = [];

  sheet.clear();

//  2060ms
// for (i = 1; i <= 1000; i++) {
//   sheet.getRange(i, 1).setValue(names[Math.floor(Math.random() * names.length)]);
//   sheet.getRange(i, 2).setValue(Math.floor(Math.random() * 101));
// }

//  123ms
  for (i = 1; i <= 1000; i++) {
    scores.push([
      names[Math.floor(Math.random() * names.length)],
      Math.floor(Math.random() * 101)
    ]);
  }

  sheet.getRange(1, 1, 1000, 2).setValues(scores);

  Logger.log(new Date() - startTime);
}






// ===== video_summary =====
スプレッドシートの値を、条件演算子を使って判定する処理を実装していきます。


    showResults()の作成

// ===== コード.gs =====
function initSheet() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var names = ['taguchi', 'fkoji', 'dotinstall'];
  var i;
  var scores = [];

  sheet.clear();

  for (i = 1; i <= 10; i++) {
    scores.push([
      names[Math.floor(Math.random() * names.length)],
      Math.floor(Math.random() * 101)
    ]);
  }

  sheet.getRange(1, 1, 10, 2).setValues(scores);
}

function showResults() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var scores = [];
  var i;
  var results = [];

//  scores = sheet.getRange(1, 2, 10, 1).getValues();
  scores = sheet.getRange(1, 2, sheet.getLastRow(), 1).getValues();

  for (i = 0; i < scores.length; i++) {
    results.push([scores[i] >= 80 ? 'PASS' : 'FAIL']);
  }
  sheet.getRange(1, 3, results.length, 1).setValues(results);
}






// ===== video_summary =====
スプレッドシート上に作成したボタンにスクリプトを割り当てる方法について見ていきます。


    初期化/判定ボタンの作成
    スクリプトの割り当て

// ===== コード.gs =====
function initSheet() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var names = ['taguchi', 'fkoji', 'dotinstall'];
  var i;
  var scores = [];

  sheet.clear();

  for (i = 1; i <= 10; i++) {
    scores.push([
      names[Math.floor(Math.random() * names.length)],
      Math.floor(Math.random() * 101)
    ]);
  }

  sheet.getRange(1, 1, 10, 2).setValues(scores);
}

function showResults() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var scores = [];
  var i;
  var results = [];

  scores = sheet.getRange(1, 2, sheet.getLastRow(), 1).getValues();

  for (i = 0; i < scores.length; i++) {
    results.push([scores[i] >= 80 ? 'PASS' : 'FAIL']);
  }
  sheet.getRange(1, 3, results.length, 1).setValues(results);
}






// ===== video_summary =====
初期化処理を行う際に、ユーザーに確認メッセージを出す方法について見ていきます。


    msgBox()
    ボタンの消去

// ===== コード.gs =====
function initSheet() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var names = ['taguchi', 'fkoji', 'dotinstall'];
  var i;
  var scores = [];

  if (Browser.msgBox('シートの初期化', '実行していいですか？', Browser.Buttons.OK_CANCEL) === 'cancel') {
    return;
  }

  sheet.clear();

  for (i = 1; i <= 10; i++) {
    scores.push([
      names[Math.floor(Math.random() * names.length)],
      Math.floor(Math.random() * 101)
    ]);
  }

  sheet.getRange(1, 1, 10, 2).setValues(scores);
}

function showResults() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var scores = [];
  var i;
  var results = [];

  scores = sheet.getRange(1, 2, sheet.getLastRow(), 1).getValues();

  for (i = 0; i < scores.length; i++) {
    results.push([scores[i] >= 80 ? 'PASS' : 'FAIL']);
  }
  sheet.getRange(1, 3, results.length, 1).setValues(results);
}






// ===== video_summary =====
スプレッドシートのメニューに自分で作った関数を表示させる方法について見ていきます。


    メニューへの追加
    セパレーターの表示

// ===== コード.gs =====
function initSheet() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var names = ['taguchi', 'fkoji', 'dotinstall'];
  var i;
  var scores = [];

  sheet.clear();

  for (i = 1; i <= 10; i++) {
    scores.push([
      names[Math.floor(Math.random() * names.length)],
      Math.floor(Math.random() * 101)
    ]);
  }

  sheet.getRange(1, 1, 10, 2).setValues(scores);
}

function showResults() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var scores = [];
  var i;
  var results = [];

  scores = sheet.getRange(1, 2, sheet.getLastRow(), 1).getValues();

  for (i = 0; i < scores.length; i++) {
    results.push([scores[i] >= 80 ? 'PASS' : 'FAIL']);
  }
  sheet.getRange(1, 3, results.length, 1).setValues(results);
}

function onOpen() {
  var spreadsheet = SpreadsheetApp.getActiveSpreadsheet();
  var items = [
    {name: '初期化', functionName: 'initSheet'},
    null,
    {name: '判定', functionName: 'showResults'}
  ];

  spreadsheet.addMenu('スコア管理', items);
}






// ===== video_summary =====
今までの知識をもとに、タスク管理アプリを作成していきます。


    MyTaskAppの作成
    Googleフォームの作成
    MyTaskProjectの作成

// ===== コード.gs =====
function sendTask() {
  
}






// ===== video_summary =====
トリガーを使って、フォームに回答が送信されたタイミングでメールが届くようにしていきます。


    トリガーの追加
    動作確認

// ===== commentBox 1 =====
補足情報
投稿内容がスプレッドシートに表示されない場合について




2018年8月7日





動画の手順でスプレッドシートからフォームを作成した場合、投稿内容の反映に時間がかかる場合があります。
少し時間を置いてご確認ください。

また、先にフォームを作成したのちに、連携するスプレッドシートを開くと反映が早くなります。
反映が遅い場合はこちらもお試しください。

// ===== コード.gs =====
function sendTask(e) {
  MailApp.sendEmail('iamdotinstall@gmail.com', 'タスクが追加されました', e.namedValues['Task']);
}






// ===== video_summary =====
タスク一覧をウェブページとして公開する方法について見ていきます。


    ウェブページの公開
    公開設定

// ===== コード.gs =====
function sendTask(e) {
  MailApp.sendEmail('iamdotinstall@gmail.com', 'タスクが追加されました', e.namedValues['Task']);
}

function doGet() {
  return HtmlService.createHtmlOutput('<h1>My Task</h1>');
}






// ===== video_summary =====
タスク一覧をHTMLファイルで表示させていきます。


    index.htmlの作成
    ブラウザでの確認

// ===== index.html =====
<!DOCTYPE html>
<html>
  <head>
    <base target="_top">
  </head>
  <body>
    <h1>My Task</h1>
    <ul>
      <li>Task</li>
      <li>Task</li>
      <li>Task</li>
    </ul>
  </body>
</html>

// ===== コード.gs =====
function sendTask(e) {
  MailApp.sendEmail('iamdotinstall@gmail.com', 'タスクが追加されました', e.namedValues['Task']);
}

function doGet() {
  var template = HtmlService.createTemplateFromFile('index');

  return template.evaluate();
}






// ===== video_summary =====
HTMLファイルに値を埋め込む方法について見ていきます。


    値の埋め込み
    動作確認

// ===== index.html =====
<!DOCTYPE html>
<html>
  <head>
    <base target="_top">
  </head>
  <body>
    <h1><?= title ?></h1>
    <ul>
      <? for (var i = 0; i < tasks.length; i++) { ?>
      <li><?= tasks[i] ?></li>
      <? } ?>
    </ul>
  </body>
</html>

// ===== コード.gs =====
function sendTask(e) {
  MailApp.sendEmail('iamdotinstall@gmail.com', 'タスクが追加されました', e.namedValues['Task']);
}

function doGet() {
  var template = HtmlService.createTemplateFromFile('index');

  template.title = 'MyTaskApp';
  template.tasks = getTasks();

  return template.evaluate();
}

function getTasks() {
  var sheet = SpreadsheetApp.getActiveSheet();

  return sheet.getRange(2, 2, sheet.getLastRow() - 1, 1).getValues();
}





// ===== video_summary =====
ウェブページにタスク追加用フォームのURLを埋め込んでいきます。


    URLの埋め込みと注意点
    Developerツールでの確認

// ===== index.html =====
<!DOCTYPE html>
<html>
  <head>
    <base target="_top">
  </head>
  <body>
    <h1><?= title ?></h1>
    <p>→ <a href="https://docs.google.com/forms/d/e/1FAIpQLSeuUlLW-SD87ebE1u8eskWaw2oK7qr45GZI2jloQabs-0hzIw/viewform">タスク追加用フォームへ</a></p>
    <ul>
      <? for (var i = 0; i < tasks.length; i++) { ?>
        <li><?= tasks[i] ?></li>
      <? } ?>
    </ul>
  </body>
</html>

// ===== コード.gs =====
function sendTask(e) {
  MailApp.sendEmail('iamdotinstall@gmail.com', 'タスクが追加されました', e.namedValues['Task']);
}

function doGet() {
  var template = HtmlService.createTemplateFromFile('index');

  template.title = 'MyTaskApp';
  template.tasks = getTasks();

  return template.evaluate();
}

function getTasks() {
  var sheet = SpreadsheetApp.getActiveSheet();

  return sheet.getRange(2, 2, sheet.getLastRow() - 1, 1).getValues();
}







// ===== video_summary =====
タスクの一覧が記載されたメールが届くようにしていきます。


    タスク一覧のメール通知設定
    動作確認

// ===== コード.gs =====
function sendTask(e) {
  MailApp.sendEmail('iamdotinstall@gmail.com', 'タスクが追加されました', e.namedValues['Task']);
}

function doGet() {
  var template = HtmlService.createTemplateFromFile('index');

  template.title = 'MyTaskApp';
  template.tasks = getTasks();

  return template.evaluate();
}

function getTasks() {
  var sheet = SpreadsheetApp.getActiveSheet();

  return sheet.getRange(2, 2, sheet.getLastRow() - 1, 1).getValues();
}

function sendReport() {
  var to = 'iamdotinstall@gmail.com';
  var subject = 'タスク一覧';
  var url = 'https://script.google.com/macros/s/AKfycbxaK66lSA4qx8vZ-Z8bG4_BL_q-4X9RrCb_2VbcGt-9yGL84iQJ/exec';
  var body = getTasks().join('\n') + '\n\n' + url;

  MailApp.sendEmail(to, subject, body);
}

// ===== index.html =====
<!DOCTYPE html>
<html>
  <head>
    <base target="_top">
  </head>
  <body>
    <h1><?= title ?></h1>
    <p>→ <a href="https://docs.google.com/forms/d/e/1FAIpQLSeuUlLW-SD87ebE1u8eskWaw2oK7qr45GZI2jloQabs-0hzIw/viewform">タスク追加用フォームへ</a></p>
    <ul>
      <? for (var i = 0; i < tasks.length; i++) { ?>
        <li><?= tasks[i] ?></li>
      <? } ?>
    </ul>
  </body>
</html>






// ===== video_summary =====
前回の変更内容を公開用URLに反映させたのちに、タスク一覧のメールが毎朝届くようにしていきます。


    変更内容を公開用URLに反映する方法
    トリガーの追加

// ===== index.html =====
<!DOCTYPE html>
<html>
  <head>
    <base target="_top">
  </head>
  <body>
    <h1><?= title ?></h1>
    <p>→ <a href="https://docs.google.com/forms/d/e/1FAIpQLSeuUlLW-SD87ebE1u8eskWaw2oK7qr45GZI2jloQabs-0hzIw/viewform">タスク追加用フォームへ</a></p>
    <ul>
      <? for (var i = 0; i < tasks.length; i++) { ?>
        <li><?= tasks[i] ?></li>
      <? } ?>
    </ul>
  </body>
</html>

// ===== コード.gs =====
function sendTask(e) {
  MailApp.sendEmail('iamdotinstall@gmail.com', 'タスクが追加されました', e.namedValues['Task']);
}

function doGet() {
  var template = HtmlService.createTemplateFromFile('index');

  template.title = 'MyTaskApp';
  template.tasks = getTasks();

  return template.evaluate();
}

function getTasks() {
  var sheet = SpreadsheetApp.getActiveSheet();

  return sheet.getRange(2, 2, sheet.getLastRow() - 1, 1).getValues();
}

function sendReport() {
  var to = 'iamdotinstall@gmail.com';
  var subject = 'タスク一覧';
  var url = 'https://script.google.com/macros/s/AKfycbxaK66lSA4qx8vZ-Z8bG4_BL_q-4X9RrCb_2VbcGt-9yGL84iQJ/exec';
  var body = getTasks().join('\n') + '\n\n' + url;

  MailApp.sendEmail(to, subject, body);
}





