// ===== video_summary =====
まずは完成版について説明したのちに、必要となる知識、レッスンにおける環境について説明していきます。


    完成版の確認
    必要となる知識
    レッスンのおける環境
    プロジェクトの作成

// ===== commentBox 1 =====
補足情報
プロジェクト作成時のUser Interfaceについて




2020年1月17日





最新版のXcodeではプロジェクト作成時にUser Interfaceを選べるようになっておりますが、レッスンと同じようにすすめるには「SwiftUI」ではなくて、「Storyboard」を選ぶようにしてください。

// ===== commentBox 2 =====
補足情報
必要となる知識について




2017年3月1日





今回のレッスンについては以下も参考にしてみてください。

・iPhoneアプリ開発入門 (全13回)
http://dotinstall.com/lessons/basic_iphoneapp_v3
・iOSレイアウト入門 (全14回)
http://dotinstall.com/lessons/basic_ios_layout_v2
・Swift 3入門 (全36回)
http://dotinstall.com/lessons/basic_swift_v2

// ===== read.me =====
iOS ストップウォッチ

# 完成版

# 知識

# 環境
- Xcode 8.1 / Swift 3

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }


}






// ===== video_summary =====
必要なラベルやボタンを配置して制約をつけていきます。


    部品の配置
    制約の設定

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }


}







// ===== video_summary =====
部品をコードから扱えるように接続していきます。


    部品の接続

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }

    @IBAction func startTimer(_ sender: Any) {
    }

    @IBAction func stopTimer(_ sender: Any) {
    }
    
    @IBAction func resetTimer(_ sender: Any) {
    }
}





// ===== video_summary =====
一定時間ごとに基準日からの経過秒数を表示する処理を実装していきます。


    タイマー処理
    動作確認

// ===== commentBox 1 =====
補足情報
Augument of '#selector' ... のエラーが出る場合




2017年11月1日





Swift 4よりselectorまわりでエラーが出るようになりました。これを修正するには、

func update() { 


を

@objc func update() {


のように修正してください。

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    func update() {
        // 2001/1/1 00:00:00
//        print(Date.timeIntervalSinceReferenceDate)
    }
    
    @IBAction func startTimer(_ sender: Any) {
        Timer.scheduledTimer(
            timeInterval: 0.01,
            target: self,
            selector: #selector(self.update),
            userInfo: nil,
            repeats: true)
    }

    @IBAction func stopTimer(_ sender: Any) {
    }
    
    @IBAction func resetTimer(_ sender: Any) {
    }
}







// ===== video_summary =====
Startボタンをタップしたときからの経過秒数を計算してみます。


    経過秒数の計算
    動作確認

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    
    var startTime: TimeInterval? = nil // Double
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    func update() {
        // 2001/1/1 00:00:00
//        print(Date.timeIntervalSinceReferenceDate)
        if let startTime = self.startTime {
            let t: Double = Date.timeIntervalSinceReferenceDate - startTime
//            print(t)
        }
    }
    
    @IBAction func startTimer(_ sender: Any) {
        self.startTime = Date.timeIntervalSinceReferenceDate
        Timer.scheduledTimer(
            timeInterval: 0.01,
            target: self,
            selector: #selector(self.update),
            userInfo: nil,
            repeats: true)
    }

    @IBAction func stopTimer(_ sender: Any) {
    }
    
    @IBAction func resetTimer(_ sender: Any) {
    }
}






// ===== video_summary =====
経過秒数を分、秒、ミリ秒に分けてラベルに表示していきます。


    分、秒、ミリ秒の計算
    ラベルの表示更新

// ===== commentBox 1 =====
補足情報
ミリ秒表示の不具合について




2017年5月26日





今回のコードにはミリ秒の表示に不具合がありますが、最後のレッスンで修正していますのでそのまま次のレッスンへ進んで構いません。

自信のある方は、最後のレッスンを見ずに修正にチャレンジしてみるのもいいでしょう。

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    
    var startTime: TimeInterval? = nil // Double
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    func update() {
        // 2001/1/1 00:00:00
//        print(Date.timeIntervalSinceReferenceDate)
        if let startTime = self.startTime {
            let t: Double = Date.timeIntervalSinceReferenceDate - startTime
//            print(t)
            let min = Int(t / 60)
            let sec = Int(t) % 60
            let msec = Int((t - Double(sec)) * 100.0)
            self.timerLabel.text = String(format: "%02d:%02d:%02d", min, sec, msec)
        }
    }
    
    @IBAction func startTimer(_ sender: Any) {
        self.startTime = Date.timeIntervalSinceReferenceDate
        Timer.scheduledTimer(
            timeInterval: 0.01,
            target: self,
            selector: #selector(self.update),
            userInfo: nil,
            repeats: true)
    }

    @IBAction func stopTimer(_ sender: Any) {
    }
    
    @IBAction func resetTimer(_ sender: Any) {
    }
}





// ===== video_summary =====
Stopボタン、Resetボタンをタップしたときの処理を実装していきｊます。


    Stop処理の実装
    Rest処理の実装

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    
    var startTime: TimeInterval? = nil // Double
    var timer = Timer()
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    func update() {
        // 2001/1/1 00:00:00
//        print(Date.timeIntervalSinceReferenceDate)
        if let startTime = self.startTime {
            let t: Double = Date.timeIntervalSinceReferenceDate - startTime
//            print(t)
            let min = Int(t / 60)
            let sec = Int(t) % 60
            let msec = Int((t - Double(sec)) * 100.0)
            self.timerLabel.text = String(format: "%02d:%02d:%02d", min, sec, msec)
        }
    }
    
    @IBAction func startTimer(_ sender: Any) {
        self.startTime = Date.timeIntervalSinceReferenceDate
        self.timer = Timer.scheduledTimer(
            timeInterval: 0.01,
            target: self,
            selector: #selector(self.update),
            userInfo: nil,
            repeats: true)
    }

    @IBAction func stopTimer(_ sender: Any) {
        self.timer.invalidate()
    }
    
    @IBAction func resetTimer(_ sender: Any) {
        self.startTime = nil
        self.timerLabel.text = "00:00:00"
    }
}






// ===== video_summary =====
一度停止したタイマーを再開させるための処理を実装していきます。


    タイマーが動いていた時間の計算
    タイマーの再開処理

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    
    var startTime: TimeInterval? = nil // Double
    var timer = Timer()
    var elapsedTime: Double = 0.0
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    func update() {
        // 2001/1/1 00:00:00
//        print(Date.timeIntervalSinceReferenceDate)
        if let startTime = self.startTime {
            let t: Double = Date.timeIntervalSinceReferenceDate - startTime + self.elapsedTime
//            print(t)
            let min = Int(t / 60)
            let sec = Int(t) % 60
            let msec = Int((t - Double(sec)) * 100.0)
            self.timerLabel.text = String(format: "%02d:%02d:%02d", min, sec, msec)
        }
    }
    
    @IBAction func startTimer(_ sender: Any) {
        self.startTime = Date.timeIntervalSinceReferenceDate
        self.timer = Timer.scheduledTimer(
            timeInterval: 0.01,
            target: self,
            selector: #selector(self.update),
            userInfo: nil,
            repeats: true)
    }

    @IBAction func stopTimer(_ sender: Any) {
        if let startTime = self.startTime {
            self.elapsedTime += Date.timeIntervalSinceReferenceDate - startTime
        }
        self.timer.invalidate()
    }
    
    @IBAction func resetTimer(_ sender: Any) {
        self.startTime = nil
        self.timerLabel.text = "00:00:00"
        self.elapsedTime = 0.0
    }
}






// ===== video_summary =====
ボタンの有効/無効化を設定するためにボタンを接続していきます。


    ボタンの有効/無効化
    ボタンの接続

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    @IBOutlet weak var startButton: UIButton!
    @IBOutlet weak var stopButton: UIButton!
    @IBOutlet weak var resetButton: UIButton!
    
    var startTime: TimeInterval? = nil // Double
    var timer = Timer()
    var elapsedTime: Double = 0.0
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        // start: true, stop: false, reset: false
        
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    func update() {
        // 2001/1/1 00:00:00
//        print(Date.timeIntervalSinceReferenceDate)
        if let startTime = self.startTime {
            let t: Double = Date.timeIntervalSinceReferenceDate - startTime + self.elapsedTime
//            print(t)
            let min = Int(t / 60)
            let sec = Int(t) % 60
            let msec = Int((t - Double(sec)) * 100.0)
            self.timerLabel.text = String(format: "%02d:%02d:%02d", min, sec, msec)
        }
    }
    
    @IBAction func startTimer(_ sender: Any) {
        // start: false, stop: true, reset: false

        self.startTime = Date.timeIntervalSinceReferenceDate
        self.timer = Timer.scheduledTimer(
            timeInterval: 0.01,
            target: self,
            selector: #selector(self.update),
            userInfo: nil,
            repeats: true)
    }

    @IBAction func stopTimer(_ sender: Any) {
        // start: true, stop: false, reset: true

        if let startTime = self.startTime {
            self.elapsedTime += Date.timeIntervalSinceReferenceDate - startTime
        }
        self.timer.invalidate()
    }
    
    @IBAction func resetTimer(_ sender: Any) {
        // start: true, stop: false, reset: false

        self.startTime = nil
        self.timerLabel.text = "00:00:00"
        self.elapsedTime = 0.0
    }
}





// ===== video_summary =====
ボタンの有効/無効化処理を実装してストップウォッチを完成させていきます。


    ボタンの有効/無効化処理の実装
    動作確認

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    @IBOutlet weak var startButton: UIButton!
    @IBOutlet weak var stopButton: UIButton!
    @IBOutlet weak var resetButton: UIButton!
    
    var startTime: TimeInterval? = nil // Double
    var timer = Timer()
    var elapsedTime: Double = 0.0
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        // start: true, stop: false, reset: false
        setButtonEnabled(start: true, stop: false, reset: false)
        
        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    func setButtonEnabled(start: Bool, stop: Bool, reset: Bool) {
        self.startButton.isEnabled = start
        self.stopButton.isEnabled = stop
        self.resetButton.isEnabled = reset
    }

    func update() {
        // 2001/1/1 00:00:00
//        print(Date.timeIntervalSinceReferenceDate)
        if let startTime = self.startTime {
            let t: Double = Date.timeIntervalSinceReferenceDate - startTime + self.elapsedTime
//            print(t)
            let min = Int(t / 60)
            let sec = Int(t) % 60
            let msec = Int((t - Double(sec)) * 100.0)
            self.timerLabel.text = String(format: "%02d:%02d:%02d", min, sec, msec)
        }
    }
    
    @IBAction func startTimer(_ sender: Any) {
        // start: false, stop: true, reset: false
        setButtonEnabled(start: false, stop: true, reset: false)

        self.startTime = Date.timeIntervalSinceReferenceDate
        self.timer = Timer.scheduledTimer(
            timeInterval: 0.01,
            target: self,
            selector: #selector(self.update),
            userInfo: nil,
            repeats: true)
    }

    @IBAction func stopTimer(_ sender: Any) {
        // start: true, stop: false, reset: true
        setButtonEnabled(start: true, stop: false, reset: true)

        if let startTime = self.startTime {
            self.elapsedTime += Date.timeIntervalSinceReferenceDate - startTime
        }
        self.timer.invalidate()
    }
    
    @IBAction func resetTimer(_ sender: Any) {
        // start: true, stop: false, reset: false
        setButtonEnabled(start: true, stop: false, reset: false)

        self.startTime = nil
        self.timerLabel.text = "00:00:00"
        self.elapsedTime = 0.0
    }
}






// ===== video_summary =====
60秒を超えるとミリ秒表示がおかしくなる不具合を直していきます。


    不具合の修正

// ===== /StopWatchApp/StopWatchApp/ViewController.swift =====
//
//  ViewController.swift
//  StopWatchApp
//
//  Created by Gen Taguchi on 2017/01/23.
//  Copyright © 2017年 Dotinstall. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var timerLabel: UILabel!
    @IBOutlet weak var startButton: UIButton!
    @IBOutlet weak var stopButton: UIButton!
    @IBOutlet weak var resetButton: UIButton!

    var startTime: TimeInterval? = nil // Double
    var timer = Timer()
    var elapsedTime: Double = 0.0

    override func viewDidLoad() {
        super.viewDidLoad()

        // start: true, stop: false, reset: false
        setButtonEnabled(start: true, stop: false, reset: false)

        // Do any additional setup after loading the view, typically from a nib.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }

    func setButtonEnabled(start: Bool, stop: Bool, reset: Bool) {
        self.startButton.isEnabled = start
        self.stopButton.isEnabled = stop
        self.resetButton.isEnabled = reset
    }

    func update() {
        // 2001/1/1 00:00:00
//        print(Date.timeIntervalSinceReferenceDate)
        if let startTime = self.startTime {
            let t: Double = Date.timeIntervalSinceReferenceDate - startTime + self.elapsedTime
//            print(t)
            let min = Int(t / 60)
            let sec = Int(t) % 60
            let msec = Int((t - Double(min * 60) - Double(sec)) * 100.0)
            self.timerLabel.text = String(format: "%02d:%02d:%02d", min, sec, msec)
        }
    }

    @IBAction func startTimer(_ sender: Any) {
        // start: false, stop: true, reset: false
        setButtonEnabled(start: false, stop: true, reset: false)

        self.startTime = Date.timeIntervalSinceReferenceDate
        self.timer = Timer.scheduledTimer(
            timeInterval: 0.01,
            target: self,
            selector: #selector(self.update),
            userInfo: nil,
            repeats: true)
    }

    @IBAction func stopTimer(_ sender: Any) {
        // start: true, stop: false, reset: true
        setButtonEnabled(start: true, stop: false, reset: true)

        if let startTime = self.startTime {
            self.elapsedTime += Date.timeIntervalSinceReferenceDate - startTime
        }
        self.timer.invalidate()
    }

    @IBAction func resetTimer(_ sender: Any) {
        // start: true, stop: false, reset: false
        setButtonEnabled(start: true, stop: false, reset: false)

        self.startTime = nil
        self.timerLabel.text = "00:00:00"
        self.elapsedTime = 0.0
    }
}






