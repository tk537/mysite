<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <pre>
Reactの概要および特徴、学習のコツについて説明していきます。

概要
特徴
学習のコツ
補足情報 Reactの公式サイト
2023年7月21日
Reactの公式サイトは以下となります。なにかわからないことがあれば、まずは公式サイトで調べてみましょう。

・React
https://react.dev/





macOSで学習環境を整える手順について説明していきます。

完成版の確認
素材ファイルのダウンロード
作業用フォルダの設定
index.htmlの表示
補足情報 VS Codeでファイルのドラッグ＆ドロップができない場合
2024年2月16日
VS Codeのバージョンアップにより、index.htmlのタブをブラウザへドラッグ＆ドロップできなくなったようです。代替手段として、下図のようにサイドバーにてファイル一覧を開き、そこからドラッグ＆ドロップするようにしてください。

macOS VS Code ドラッグ&ドロップ イメージ

それでもうまくいかない場合は、Chromeブラウザを選択した状態で、commandキーを押しながら、英語のoキーを押すと、ファイルを選択できるようになります。そこからファイルを開いても OK です。

補足情報 HTMLの雛形ファイルについて
2023年7月25日
レッスン素材として配布していた index.html にコード抜けがありました。2 行目に以下のコードがない場合、追加してから学習を進めてみてください。

<html lang="ja">

補足情報 動画内で紹介しているURLについて
2023年7月21日
動画内で紹介しているURLは以下のとおりです。完成版の確認、素材ファイルのダウンロードは以下よりアクセスしてみてください。

・ドットインストールでプログラミングを始めよう: React入門
https://dotinstall.github.io/setup/basic_reactjs_v2.html

<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
</head>
<body>
  <h1>My React App</h1>
  
  <script type="text/babel">
  </script>
</body>
</html>




Windowsで学習環境を整える手順について説明していきます。

完成版の確認
素材ファイルのダウンロード
作業用フォルダの設定
index.htmlの表示
補足情報 VS Codeでファイルのドラッグ＆ドロップができない場合
2024年2月16日
VS Codeのバージョンアップにより、index.htmlのタブをブラウザへドラッグ＆ドロップできなくなったようです。代替手段として、下図のようにサイドバーにてファイル一覧を開き、そこからドラッグ＆ドロップするようにしてください。

Windows VS Code ドラッグ&ドロップ イメージ

それでもうまくいかない場合は、Chromeブラウザを選択した状態で、Ctrlキーを押しながら、英語のoキーを押すと、ファイルを選択できるようになります。そこからファイルを開いても OK です。

補足情報 動画内で紹介しているURLについて
2023年7月21日
動画内で紹介しているURLは以下のとおりです。完成版の確認、素材ファイルのダウンロードは以下よりアクセスしてみてください。

・ドットインストールでプログラミングを始めよう: React入門
https://dotinstall.github.io/setup/basic_reactjs_v2.html

<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
</head>
<body>
  <h1>My React App</h1>
  
  <script type="text/babel">
  </script>
</body>
</html>




Reactを使ってh1要素を画面に描画する方法をみていきます。

div要素の配置
ReactDOM.createRoot()
React.createElement()

<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(
        React.createElement('h1', null, 'メニュー')
      );
    }
  </script>
</body>
</html>




描画する要素をHTMLのように記述できる、JSXという記法について見ていきます。

JSXによる書き換え
JSXのルール
<></>

<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(
        <>
          <h1>メニュー</h1>
          <ul>
            <li>牛丼</li>
            <li>牛丼</li>
            <li>牛丼</li>
          </ul>
          <p>合計: 0円</p>
        </>
      );
    }
  </script>
</body>
</html>




CSSファイルを作成して、スタイリングを進めていきます。

style.cssの作成
余白のリセット
#containerのスタイリング

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(
        <>
          <h1>メニュー</h1>
          <ul>
            <li>牛丼</li>
            <li>牛丼</li>
            <li>牛丼</li>
          </ul>
          <p>合計: 0円</p>
        </>
      );
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
  background: pink;
}

h1 {
  margin: 0;
  font-size: 20px;
  text-align: center;
}

ul {
  margin: 0;
  padding: 0;
  list-style: none;
}

p {
  margin: 0;
  text-align: right;
}




SX内でclass属性を表現するための手法について見ていきます。

className
li要素のスタイリング
余白の設定
補足情報 子結合子（>）について
2023年7月21日
動画内で使っている子結合子（>）については以下を復習してみてください。

・#11 子結合子､子孫結合子を使ってみよう | CSS入門 セレクター編
https://dotinstall.com/lessons/basic_css_selectors_v2/62111

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(
        <>
          <h1>メニュー</h1>
          <ul className="menus">
            <li>牛丼</li>
            <li>牛丼</li>
            <li>牛丼</li>
          </ul>
          <p>合計: 0円</p>
        </>
      );
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}




アプリケーション全体をAppというコンポーネントとして切り出す方法について見ていきます。

Appコンポーネント
コンポーネントのルール
閉じタグに関する注意点

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const App = () => {
        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              <li>牛丼</li>
              <li>牛丼</li>
              <li>牛丼</li>
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}




リストの項目をMenuコンポーネントとして切り出し、propsで値を受け取る方法について見ていきます。

Menuコンポーネント
属性の設定
props
コンソールでの確認
補足情報 「Download the React DevTools for ...」というメッセージについて
2023年7月21日
コンソールに「Download the React DevTools for ...」というメッセージが表示されるかもしれませんが、こちらは「React開発用の拡張機能を入れたほうがいいですよ」という意味になります。ただ、レッスン中では使用していないので気にしなくてOKです。

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        console.log(props);
        return (
          <li>牛丼</li>
        );
      };

      const App = () => {
        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              <Menu name="牛丼" />
              <Menu name="カレー" />
              <Menu name="サラダ" />
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>


style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}




propsで受け取った値を{}で埋め込む方法について見ていきます。

{}
価格の表示
ボタンのスタイリング
補足情報 子孫結合子（半角空白）について
2023年7月21日
動画内で使っている子孫結合子（半角空白）については以下を復習してみてください。

・#11 子結合子､子孫結合子を使ってみよう | CSS入門 セレクター編
https://dotinstall.com/lessons/basic_css_selectors_v2/62111

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        return (
          <li>
            <button>-</button>
            <button>+</button>
            {props.name} ({props.price}円 ☓ 0個)
          </li>
        );
      };

      const App = () => {
        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              <Menu name="牛丼" price="400" />
              <Menu name="カレー" price="500" />
              <Menu name="サラダ" price="300" />
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}





配列のデータをもとにコンポーネントを生成するための方法を見ていきます。

配列データの準備
map()
警告の確認
補足情報 map()について
2023年7月21日
動画内で使っているmap()については以下の動画を復習してみましょう。

・#18 map()で新しい配列を作ってみよう | JavaScript入門 データ構造編
https://dotinstall.com/lessons/basic_javascript_data_structure/63618

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        return (
          <li>
            <button>-</button>
            <button>+</button>
            {props.name} ({props.price}円 ☓ 0個)
          </li>
        );
      };

      const App = () => {
        const menus = [
          {name: '牛丼', price: 400},
          {name: 'カレー', price: 500},
          {name: 'サラダ', price: 300},
        ];

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              name={menu.name}
              price={menu.price}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}






警告の内容にしたがって、リスト項目にkey属性を設定していきます。

idの設定
key属性の設定

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        return (
          <li>
            <button>-</button>
            <button>+</button>
            {props.name} ({props.price}円 ☓ 0個)
          </li>
        );
      };

      const App = () => {
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              name={menu.name}
              price={menu.price}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}




ユーザーの操作によって変化する値をuseState()で管理していく方法について見ていきます。

React.useState()
constで宣言して良い理由
個数の表示
補足情報 分割代入について
2023年7月21日
動画内で使っている分割代入については以下の動画を復習してみましょう。

・#20 分割代入を使ってみよう | JavaScript入門 データ構造編
https://dotinstall.com/lessons/basic_javascript_data_structure/63620

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const [count, setCount] = React.useState(0);

        return (
          <li>
            <button>-</button>
            <button>+</button>
            {props.name} ({props.price}円 ☓ {count}個)
          </li>
        );
      };

      const App = () => {
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              name={menu.name}
              price={menu.price}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}





ボタンにイベントリスナーを追加して動作させていきます。

onClick属性
decrement関数の定義
increment関数の定義
条件分岐

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const [count, setCount] = React.useState(0);
        const decrement = () => {
          if (count > 0) {
            setCount(count - 1);
          }
        };
        const increment = () => {
          setCount(count + 1);
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {count}個)
          </li>
        );
      };

      const App = () => {
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              name={menu.name}
              price={menu.price}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}





useState()で管理している値に関する注意点について説明していきます。

更新タイミング
直前で更新された値を利用する方法

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const [count, setCount] = React.useState(0);
        const decrement = () => {
          if (count > 0) {
            setCount(count - 1);
          }
        };
        const increment = () => {
          setCount(count + 1);
          setCount((prevCount) => { return prevCount + 1; });
          setCount((prevCount) => { return prevCount + 1; });
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {count}個)
          </li>
        );
      };

      const App = () => {
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              name={menu.name}
              price={menu.price}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}



合計金額を計算するために、Menuコンポーネントで管理していた値をAppコンポーネントに移動します。

データの管理方法
counts、setCountsの宣言
個数の表示

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const decrement = () => {
        };
        const increment = () => {
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {props.count}個)
          </li>
        );
      };

      const App = () => {
        const [counts, setCounts] = React.useState([0, 0, 0]);
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              count={counts[menu.id]}
              name={menu.name}
              price={menu.price}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: 0円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}





Appコンポーネントで合計金額を計算して表示する方法について見ていきます。

合計金額の計算、表示
設計に関する注意点
index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const decrement = () => {
        };
        const increment = () => {
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {props.count}個)
          </li>
        );
      };

      const App = () => {
        const [counts, setCounts] = React.useState([1, 1, 1]);
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];
        const total = 
          (menus[0].price * counts[0]) + 
          (menus[1].price * counts[1]) + 
          (menus[2].price * counts[2]);

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              count={counts[menu.id]}
              name={menu.name}
              price={menu.price}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: {total}円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>


style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}






ボタンを動作させるための実装方法について図で確認していきます。

実装方法の確認
decrement関数
decrementMenu関数
onDecrement属性
props.onDecrement()

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const decrement = () => {
        };
        const increment = () => {
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {props.count}個)
          </li>
        );
      };

      const App = () => {
        const [counts, setCounts] = React.useState([0, 0, 0]);
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];
        const total = 
          (menus[0].price * counts[0]) + 
          (menus[1].price * counts[1]) + 
          (menus[2].price * counts[2]);

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              count={counts[menu.id]}
              name={menu.name}
              price={menu.price}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: {total}円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}





-のボタンをクリックしたときの実装を進めていきます。

decrementMenu関数の実装
onDecrement属性の設定
props.onDecrement()

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const decrement = () => {
          // App component function
          props.onDecrement();
        };
        const increment = () => {
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {props.count}個)
          </li>
        );
      };

      const App = () => {
        const [counts, setCounts] = React.useState([0, 0, 0]);
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];
        const total = 
          (menus[0].price * counts[0]) + 
          (menus[1].price * counts[1]) + 
          (menus[2].price * counts[2]);
        const decrementMenu = () => {
          console.log('minus button clicked!');
        };

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              count={counts[menu.id]}
              name={menu.name}
              price={menu.price}
              onDecrement={decrementMenu}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: {total}円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}






+のボタンをクリックしたときの実装を進めていきます。

incrementMenu関数の実装
onIncrement属性の設定
props.onIncrement()

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const decrement = () => {
          // App component function
          props.onDecrement();
        };
        const increment = () => {
          props.onIncrement();
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {props.count}個)
          </li>
        );
      };

      const App = () => {
        const [counts, setCounts] = React.useState([0, 0, 0]);
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];
        const total = 
          (menus[0].price * counts[0]) + 
          (menus[1].price * counts[1]) + 
          (menus[2].price * counts[2]);
        const decrementMenu = () => {
          console.log('minus button clicked!');
        };
        const incrementMenu = () => {
          console.log('plus button clicked!');
        };

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              count={counts[menu.id]}
              name={menu.name}
              price={menu.price}
              onDecrement={decrementMenu}
              onIncrement={incrementMenu}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: {total}円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}




仮引数を設定したうえで、countsを更新するための処理を実装していきます。

setCounts()
配列のコピー
仮引数の設定
countsの更新
補足情報 配列のコピーについて
2023年7月21日
配列のコピーを作る際の注意点については以下の動画を復習してみましょう。

・#27 スプレッド構文で値のコピーを作ろう | JavaScript入門 データ構造編
https://dotinstall.com/lessons/basic_javascript_data_structure/63627

補足情報 仮引数、実引数について
2023年7月21日
仮引数、実引数については以下の動画を復習してみましょう。

・#02 関数を作ってみよう | JavaScript入門 関数編
https://dotinstall.com/lessons/basic_javascript_functions/63502

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const decrement = () => {
          // App component function
          props.onDecrement();
        };
        const increment = () => {
          props.onIncrement();
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {props.count}個)
          </li>
        );
      };

      const App = () => {
        const [counts, setCounts] = React.useState([0, 0, 0]);
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];
        const total = 
          (menus[0].price * counts[0]) + 
          (menus[1].price * counts[1]) + 
          (menus[2].price * counts[2]);
        const decrementMenu = (menuId) => {
          // [0, 0, 0] → [-1, 0, 0]
          const newCounts = [...counts];
          newCounts[menuId]--;
          setCounts(newCounts);
        };
        const incrementMenu = () => {
          console.log('plus button clicked!');
        };

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              count={counts[menu.id]}
              name={menu.name}
              price={menu.price}
              onDecrement={decrementMenu}
              onIncrement={incrementMenu}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: {total}円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}




Menuコンポーネントのdecrement関数に実引数を渡すための方法を見ていきます。

menuId属性の設定
実引数の設定

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const decrement = () => {
          // App component function
          props.onDecrement(props.menuId);
        };
        const increment = () => {
          props.onIncrement();
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {props.count}個)
          </li>
        );
      };

      const App = () => {
        const [counts, setCounts] = React.useState([0, 0, 0]);
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];
        const total = 
          (menus[0].price * counts[0]) + 
          (menus[1].price * counts[1]) + 
          (menus[2].price * counts[2]);
        const decrementMenu = (menuId) => {
          // [0, 0, 0] → [-1, 0, 0]
          const newCounts = [...counts];
          newCounts[menuId]--;
          setCounts(newCounts);
        };
        const incrementMenu = () => {
          console.log('plus button clicked!');
        };

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              menuId={menu.id}
              count={counts[menu.id]}
              name={menu.name}
              price={menu.price}
              onDecrement={decrementMenu}
              onIncrement={incrementMenu}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: {total}円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css
@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}






個数がマイナスにならないように調整したのち、+ボタンの実装を完成させていきます。

条件分岐
+ボタンの実装

index.html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My React App</title>
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="container"></div>
  
  <script type="text/babel">
    'use strict';

    {
      const Menu = (props) => {
        const decrement = () => {
          // App component function
          props.onDecrement(props.menuId);
        };
        const increment = () => {
          props.onIncrement(props.menuId);
        };

        return (
          <li>
            <button onClick={decrement}>-</button>
            <button onClick={increment}>+</button>
            {props.name} ({props.price}円 ☓ {props.count}個)
          </li>
        );
      };

      const App = () => {
        const [counts, setCounts] = React.useState([0, 0, 0]);
        const menus = [
          {id: 0, name: '牛丼', price: 400},
          {id: 1, name: 'カレー', price: 500},
          {id: 2, name: 'サラダ', price: 300},
        ];
        const total = 
          (menus[0].price * counts[0]) + 
          (menus[1].price * counts[1]) + 
          (menus[2].price * counts[2]);
        const decrementMenu = (menuId) => {
          if (counts[menuId] > 0) {
            // [0, 0, 0] → [-1, 0, 0]
            const newCounts = [...counts];
            newCounts[menuId]--;
            setCounts(newCounts);
          }
        };
        const incrementMenu = (menuId) => {
          const newCounts = [...counts];
          newCounts[menuId]++;
          setCounts(newCounts);
        };

        const menuItems = menus.map((menu) => {
          return (
            <Menu 
              key={menu.id}
              menuId={menu.id}
              count={counts[menu.id]}
              name={menu.name}
              price={menu.price}
              onDecrement={decrementMenu}
              onIncrement={incrementMenu}
            />
          );
        });

        return (
          <>
            <h1>メニュー</h1>
            <ul className="menus">
              {menuItems}
            </ul>
            <p>合計: {total}円</p>
          </>
        );
      };

      const container = document.querySelector('#container');
      const root = ReactDOM.createRoot(container);
      root.render(<App />);
    }
  </script>
</body>
</html>

style.css

@charset "utf-8";

body {
  margin: 0;
}

#container {
  width: 400px;
  margin: 0 auto;
}

h1 {
  margin: 16px 0 0 0;
  font-size: 20px;
  text-align: center;
}

.menus {
  margin: 0;
  padding: 0;
  list-style: none;
}

.menus > li {
  border: 1px solid #ccc;
  padding: 8px;
  border-radius: 8px;
  margin-top: 16px;
}

.menus button {
  margin-right: 8px;
  width: 24px;
}

p {
  margin: 16px 0 0 0;
  text-align: right;
}

</pre>
</body>
</html>




