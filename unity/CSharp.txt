// ===== video_summary =====
まずは概要を説明したのちに公式サイト、必要となる知識、レッスンにおける環境について説明していきます。


    概要
    公式サイト
    必要となる知識
    Mono

// ===== commentBox 1 =====
補足情報
必要となる知識について




2016年11月21日





今回のレッスンについては以下も参考にしてみてください。

・ローカル開発環境の構築 [macOS編] (全14回)
http://dotinstall.com/lessons/basic_localdev_mac_v2
・ローカル開発環境の構築 [Windows編] (全14回)
http://dotinstall.com/lessons/basic_localdev_win_v2
・UNIXコマンド入門 (一般ユーザー編) (全16回)
http://dotinstall.com/lessons/basic_unix

// ===== read.me =====
C#

* 概要
- Microsoft
- Windows
- Game Programming

* 公式サイト

* 知識

* 環境
- ローカル開発環境
- mono





// ===== video_summary =====
ローカル開発環境に今回使うC#コンパイラであるMonoをインストールしていきます。


    Monoのインストール

// ===== commentBox 1 =====
補足情報
Monoのインストールについて




2020年5月7日





2020年5月現在、Monoのインストール方法は以下のページの内容に従う必要があります。
https://www.mono-project.com/download/stable/#download-lin-centos
具体的には、
sudo rpm --import "http://pool.sks-keyservers.net/pks/lookup?op=get&search=0x3fa7e0328081bff6a14da29aa6a19b38d3d831ef"
を実行し次に、
su -c 'curl https://download.mono-project.com/repo/centos6-stable.repo | tee /etc/yum.repos.d/mono-centos6-stable.repo'
を実行します。こちらのコマンドの実行時にはパスワードの入力が求められるので、パスワードには vagrant と入力してください。
最後に、
sudo yum -y install mono-devel
とすればOKです。
mono --version
でバージョンが表示されるか確認してみてください。
※ 動画で実施している . /opt/mono/env.sh の実行は不要です。

// ===== read.me =====
cd /etc/yum.repos.d
sudo yum install epel-release
sudo wget https://copr.fedoraproject.org/coprs/tpokorra/mono-opt/repo/epel-6/tpokorra-mono-opt-epel-6.repo

sudo yum install monodevelop-opt nunit-opt

. /opt/mono/env.sh
mono --version

cd
mkdir csharp_lessons
cd csharp_lessons





// ===== video_summary =====
Hello Worldと表示するプログラムを書いて実行してみます。


    MyApp.csの作成
    コンパイル方法
    実行方法
    コメントの書き方

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // comment
    /*
      comment
      comment
      comment
      comment
    */
    Console.WriteLine("Hello World");
  }

}





// ===== video_summary =====
変数の使い方、型推論について見ていきます。


    変数、定数
    型推論

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // 変数: 再代入が可能
    // 定数: 再代入が不可能

    // 変数の型 変数名;
    // string msg;
    // msg = "Hello World";
    // string msg = "Hello World";
    const string msg = "Hello World";
    msg = "hello";
    Console.WriteLine(msg);
  }

}





// ===== video_summary =====
C#で扱える基本的なデータ型について例を出していきます。


    基本的なデータ型

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // 文字列、文字
    // string, char
    string s = "hello";
    char c = 'a';

    // 整数値
    // byte, short, int, long
    int i = 100;

    // 浮動小数点数
    // float, double
    double d = 52342.34;
    float f = 23.3f;

    // 論理値
    // bool -> true, false
    bool flag = true;

    // 型推論
    var x = 5; // int
    var y = "world"; // string
  }

}





// ===== video_summary =====
基本的なデータ型の演算について見ていきます。


    データの演算

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // + - * / %
    var x = 10; // int
    Console.WriteLine(x / 3); // 3
    Console.WriteLine(x % 3); // 1
    Console.WriteLine(x / 3.0); // 3.333....
    Console.WriteLine(x / (double)3); // 3.333....

    // ++ --
    var y = 5;
    y++;
    Console.WriteLine(y); // 6
    y--;
    Console.WriteLine(y); // 5

    var z = 6;
    // z = z + 10;
    z += 10;

    // AND OR NOT
    // && || !
    var flag = true;
    Console.WriteLine(!flag);
  }

}





// ===== video_summary =====
文字列の連結や、変数を展開する方法について見ていきます。


    文字列の連結
    特殊文字
    変数の展開
    式の評価
    書式指定

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    Console.WriteLine("hello " + "world");

    // \n, \t
    Console.WriteLine("hell\no wo\trld");

    var name = "taguchi";
    var score = 52.3;

    Console.WriteLine(string.Format("{0} [{1}]", name, score)); // taguchi [52.3]

    Console.WriteLine($"{name} [{score}]");
    Console.WriteLine($"{name, -10} [{score, 10}]");
    Console.WriteLine($"{name, -10} [{score, 10:0.00}]");
    Console.WriteLine($"{name, -10} [{score + 25, 10:0.00}]");
  }

}





// ===== video_summary =====
ifを使って条件分岐をする方法について解説していきます。


    条件分岐
    比較演算子

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // if
    var score = int.Parse(Console.ReadLine());

    // > >= < <= == !=
    // if (score > 80) {
    //   Console.WriteLine("Great!");
    // } else if (score > 60) {
    //   Console.WriteLine("Good!");
    // } else {
    //   Console.WriteLine("so so ...!");
    // }

    Console.WriteLine((score > 80) ? "Great" : "so so ...");
  }

}






// ===== video_summary =====
switchを使って条件分岐をする方法について見ていきます。


    switch

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // switch
    var signal = Console.ReadLine();

    switch(signal) {
      case "red":
        Console.WriteLine("Stop!");
        break;
      case "blue":
      case "green":
        Console.WriteLine("Go!");
        break;
      case "yellow":
        Console.WriteLine("Caution!");
        break;
      default:
        Console.WriteLine("wrong signal!");
        break;
    }

  }

}




// ===== video_summary =====
条件を満たしているあいだ、特定の処理を繰り返して実行する方法について見ていきます。


    while
    do ... while

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // while
    // 0 - 9
    // var i = 0;
    var i = 100;

    while (i < 10) {
      Console.WriteLine($"loop:{i}");
      i++;
    }

    do {
      Console.WriteLine($"loop2:{i}");
      i++;
    } while (i < 10);

  }

}





// ===== video_summary =====
forを使って繰り返し処理を行う方法について解説していきます。


    for
    break
    continue

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // for
    // 0 - 9

    // continue それ以降の処理を中止して次のループへ
    // break ループ自体を抜ける

    for (int i = 0; i < 10; i++) {
      // if (i == 5) {
      //   continue;
      // }
      if (i == 5) {
        break;
      }
      Console.WriteLine(i);
    }

  }

}





// ===== video_summary =====
配列を使って複数のデータを扱う方法について見ていきます。


    配列
    要素へのアクセス

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    // 配列
    // var score_1 = 10;
    // var score_2 = 30;

    // int[] scores = new int[3];
    // scores[0] = 10;
    // scores[1] = 30;
    // scores[2] = 20;
    // int[] scores = new int[] {10, 30, 20};
    // int[] scores = {10, 30, 20};
    var scores = new[] {10, 30, 20};

    scores[1] = 40;
    Console.WriteLine(scores[1]);
  }

}





// ===== video_summary =====
配列のすべての要素に対してなんらかの処理を行う方法について解説していきます。


    Length
    foreach

// ===== MyApp.cs =====
using System;

class MyApp {

  static void Main() {
    var scores = new[] {10, 30, 20};

    // for (int i = 0; i < scores.Length; i++) {
    //   Console.WriteLine(scores[i]);
    // }

    foreach (int score in scores) {
      Console.WriteLine(score);
    }

  }

}





// ===== video_summary =====
複数の処理をまとめて名前をつけることができるメソッドについて見ていきます。


    メソッド
    返り値

// ===== MyApp.cs =====
using System;

class MyApp {

  // static void SayHi() {
  //   Console.WriteLine("hi!");
  // }
  // static string SayHi() {
  //   return "hi!";
  // }
  static string SayHi() => "hi!";

  static void Main() {
    // method
    // SayHi();
    Console.WriteLine(SayHi());
  }

}






// ===== video_summary =====
メソッドに与えるオプションである引数について見ていきます。


    引数
    初期値
    名前付き引数

// ===== MyApp.cs =====
using System;

class MyApp {

  // static void SayHi(string name) { // 引数
  //   Console.WriteLine($"hi! {name}");
  // }
  static void SayHi(string name, int age = 23) {
    Console.WriteLine($"hi! {name} ({age})");
  }

  static void Main() {
    SayHi("Tom", 30); // tom 30
    SayHi("Bob"); // bob 23
    SayHi(age: 26, name: "Steve"); // steve 26
  }

}





// ===== video_summary =====
自分で作ることができるデータ型であるクラスについて見ていきます。


    クラス
    フィールド、メソッド
    インスタンス

// ===== MyApp.cs =====
using System;

// Class
// - 変数（フィールド）
// - メソッド

class User {
  public string name = "me";
  public void SayHi() {
    // Console.WriteLine($"hi {this.name}");
    Console.WriteLine($"hi {name}");
  }
}

class MyApp {

  static void Main() {
    User user = new User(); // インスタンス
    Console.WriteLine(user.name); // me
    user.SayHi(); // hi! me
    user.name = "taguchi";
    user.SayHi(); // hi! taguchi
  }

}





// ===== video_summary =====
クラスをインスタンス化するときに必ず呼び出されるコンストラクタについて見ていきます。


    コンストラクタ
    オーバーロード

// ===== MyApp.cs =====
using System;

// コンストラクタ

class User {
  public string name;
  // public User() {
  //   this.name = "ME";
  // }
  public User(string name) {
    this.name = name;
  }
  // public User() { // オーバーロード
  //   this.name = "nobody";
  // }
  public User(): this("nobody") {
  }
  public void SayHi() {
    Console.WriteLine($"hi {name}");
  }
}

class MyApp {

  static void Main() {
    // User user = new User();
    // user.SayHi();
    User tom = new User("Tom");
    tom.SayHi();
    User user = new User();
    user.SayHi();
  }

}





// ===== video_summary =====
既存のクラスをもとにして新しいクラスを作ることができる継承の仕組みについて見ていきます。


    継承
    オーバーライド

// ===== MyApp.cs =====
using System;

// 継承
// User -> AdminUser

class User {
  public string name;
  public User(string name) {
    this.name = name;
  }
  public virtual void SayHi() {
    Console.WriteLine($"hi {name}");
  }
}

class AdminUser: User {
  public AdminUser(string name): base(name) {
  }
  public void SayHello() {
    Console.WriteLine($"hello {name}");
  }
  public override void SayHi() { // override
    Console.WriteLine($"[admin] hi {name}");
  }
}

class MyApp {

  static void Main() {
    User tom = new User("Tom");
    tom.SayHi();
    AdminUser bob = new AdminUser("Bob");
    bob.SayHi();
    bob.SayHello();
  }

}





// ===== video_summary =====
クラスのメンバなどにつけてアクセスの範囲をコントロールできるアクセス修飾子について見ていきます。


    アクセス修飾子

// ===== MyApp.cs =====
using System;

// アクセス修飾子
// - public
// - protected
// - private

class User {
  // public string name;
  // protected string name;
  private string name;
  public User(string name) {
    this.name = name;
  }
  public virtual void SayHi() {
    Console.WriteLine($"hi {name}");
  }
}

class AdminUser: User {
  public AdminUser(string name): base(name) {
  }
  public void SayHello() {
    Console.WriteLine($"hello {name}");
  }
  public override void SayHi() { // override
    Console.WriteLine($"[admin] hi {name}");
  }
}

class MyApp {

  static void Main() {
    User tom = new User("Tom");
    Console.WriteLine(tom.name);
    tom.SayHi();
    AdminUser bob = new AdminUser("Bob");
    bob.SayHi();
    bob.SayHello();
  }

}





// ===== video_summary =====
フィールドに対するアクセスをコントロールすることができるプロパティについて見ていきます。


    プロパティ
    アクセスのコントロール

// ===== MyApp.cs =====
using System;

class User {
  private string name = "me!";
  // プロパティ
  public string Name {
    get { return this.name; } // getter
    set {
      if (value != "") {
        this.name = value;
      }
    } // setter
  }
  // public string Name { get; set; } = "me!";
}

class MyApp {

  static void Main() {
    User user = new User();
    Console.WriteLine(user.Name);
    user.Name = "taguchi";
    Console.WriteLine(user.Name);
    user.Name = "";
    Console.WriteLine(user.Name);
  }

}





// ===== video_summary =====
クラスのインスタンスを配列のように扱うことができるインデクサについて見ていきます。


    インデクサ
    要素へのアクセス

// ===== MyApp.cs =====
using System;

// インデクサ

class Team {
  private string[] members = new string[3];
  public string this[int i] {
    get { return this.members[i]; }
    set { this.members[i] = value; }
  }
}

class MyApp {

  static void Main() {
    Team giants = new Team();
    giants[0] = "taguchi";
    giants[1] = "fkoji";
    giants[2] = "dotinstall";
    Console.WriteLine(giants[1]);
  }

}





// ===== video_summary =====
インスタンス化せずともクラスのメンバが使えるようになるstatic修飾子について見ていきます。


    static修飾子

// ===== MyApp.cs =====
using System;

// static

class User {
  private static int count = 0;
  public User() {
    User.count++;
  }
  public static void GetCount() {
    Console.WriteLine($"# of instances: {count}");
  }
}

class MyApp {

  static void Main() {
    User.GetCount(); // 0
    User user1 = new User();
    User user2 = new User();
    User user3 = new User();
    User.GetCount(); // 3
  }

}





// ===== video_summary =====
継承することが前提になっている抽象クラスについて見ていきます。


    抽象クラス
    抽象メソッド

// ===== MyApp.cs =====
using System;

// 抽象クラス
// User -> Japanese, American

abstract class User {
  public abstract void SayHi();
}

class Japanese: User {
  public override void SayHi() {
    Console.WriteLine("こんにちは！");
  }
}

class American: User {
  public override void SayHi() {
    Console.WriteLine("hi!");
  }
}

class MyApp {

  static void Main() {
    Japanese aki = new Japanese();
    aki.SayHi();
    American tom = new American();
    tom.SayHi();
  }

}





// ===== video_summary =====
クラスに機能を実装させることを保証させるインターフェースについて見ていきます。


    インターフェースの作成
    インターフェースの実装

// ===== MyApp.cs =====
using System;

// interface

interface ISharable {
  void Share();
}

class User: ISharable {
  // public void Share() {
  //   Console.WriteLine("now sharing...");
  // }
}

class MyApp {

  static void Main() {
    User user = new User();
    user.Share();
  }

}






// ===== video_summary =====
型を汎用化させることができるジェネリックの仕組みについて解説していきます。


    ジェネリックを利用しない例
    ジェネリックを利用した例

// ===== MyApp.cs =====
using System;

// Generic

// class MyInteger {
//   public void GetThree(int x) {
//     Console.WriteLine(x);
//     Console.WriteLine(x);
//     Console.WriteLine(x);
//   }
// }

class MyData<T> {
  public void GetThree(T x) {
    Console.WriteLine(x);
    Console.WriteLine(x);
    Console.WriteLine(x);
  }
}

class MyApp {

  static void Main() {
    // MyInteger mi = new MyInteger();
    // mi.GetThree(55);
    MyData<string> s = new MyData<string>();
    s.GetThree("hello");
    MyData<double> d = new MyData<double>();
    d.GetThree(23.3);
  }

}





// ===== video_summary =====
クラス名の衝突や、クラスの分類などに使える名前空間について見ていきます。


    名前空間
    using

// ===== MyApp.cs =====
// using System;
using DotinstallNamespace;

// 名前空間

namespace DotinstallNamespace {
  class User {
    public void SayHi() {
      // Console.WriteLine("hi!");
      System.Console.WriteLine("hi!");
    }
  }
}

class MyApp {

  static void Main() {
    // DotinstallNamespace.User user = new DotinstallNamespace.User();
    User user = new User();
    user.SayHi();
  }

}





// ===== video_summary =====
単純で小さなデータを扱う際に使える構造体について見ていきます。


    構造体
    クラスとの違い

// ===== MyApp.cs =====
using System;

// 構造体
// - 継承ができない
// - メモリの使い方

struct Point {
  public int X { get; }
  public int Y { get; }
  public Point(int x, int y) {
    X = x;
    Y = y;
  }
  public void GetInfo() {
    Console.WriteLine($"({X}:{Y})");
  }
}

class MyApp {

  static void Main() {
    Point p1 = new Point(5, 3);
    Point p2 = new Point(12, 4);
    p1.GetInfo();
    p2.GetInfo();
  }

}





// ===== video_summary =====
複数の定数をまとめることができる列挙体について見ていきます。


    列挙体
    定数へのアクセス
    数値への変換

// ===== MyApp.cs =====
using System;

// 列挙型

enum Direction {
  Stay = 0,
  Right = 1,
  Left = -1
}

class MyApp {

  static void Main() {
    Direction dir = Direction.Right;
    Console.WriteLine((int)Direction.Right);

    switch (dir) {
      case Direction.Stay:
        // そのまま
        break;
      case Direction.Right:
        // 右へ
        break;
      case Direction.Left:
        // 左へ
        break;
    }
  }

}




// ===== video_summary =====
実行時に予期せぬエラーが発生した場合に使える例外について見ていきます。


    例外の捉え方
    例外クラスの作り方

// ===== MyApp.cs =====
using System;

// 例外

class MyException: Exception {
  public MyException(string msg): base(msg) {

  }
}

class MyApp {

  static void Div(int a, int b) {
    try {
      if (b < 0) {
        // 例外
        throw new MyException("not minus!");
      }
      Console.WriteLine(a / b);
    } catch (DivideByZeroException e) {
      Console.WriteLine(e.Message);
    } catch (MyException e) {
      Console.WriteLine(e.Message);
    } finally {
      Console.WriteLine(" -- end -- ");
    }
  }

  static void Main() {
    Div(10, 0);
    Div(10, -3);
  }

}





// ===== video_summary =====
メソッドを変数として扱うことができるdelegateについて解説していきます。


    delegate型の宣言
    delegateの利用
    マルチキャスト

// ===== commentBox 1 =====
補足情報
女性ボイスでの誤読について




2019年12月15日





女性ボイスの2:23〜から「マルチキャストデリート」と読んでいますが、正しくは「マルチキャストデリゲート」となります。ユーザーの方よりご指摘いただきました。ありがとうございました。

// ===== MyApp.cs =====
using System;

// delegate

delegate void MyDelegate();

class MyApp {

  // static void SayHi() {
  //   Console.WriteLine("hi!");
  // }
  static void SayHi() => Console.WriteLine("hi!");
  static void SayHello() => Console.WriteLine("hello!");

  static void Main() {

    MyDelegate ShowMessage;

    ShowMessage = SayHi;
    ShowMessage += SayHello;

    ShowMessage();

  }

}





// ===== video_summary =====
その場でメソッドを定義することができる匿名メソッド、ラムダ式について見ていきます。


    匿名メソッド
    ラムダ式

// ===== MyApp.cs =====
using System;

delegate void MyDelegate();

class MyApp {

  // static void SayHi() => Console.WriteLine("hi!");
  // static void SayHello() => Console.WriteLine("hello!");

  static void Main() {

    MyDelegate ShowMessage;

    // 匿名メソッド
    ShowMessage = delegate {
      Console.WriteLine("hi!");
    };
    // ラムダ式: 引数 => 処理
    // ShowMessage += () => {
    //   Console.WriteLine("hello!");
    // };
    ShowMessage += () => Console.WriteLine("hello!");

    ShowMessage();

  }

}





// ===== video_summary =====
イベントが発生したときになんらかの通知を行うことができる仕組みについて見ていきます。


    event
    動作確認

// ===== MyApp.cs =====
using System;

// イベント

delegate void MyEventHandler();

class MyButton {
  public event MyEventHandler MyEvent;
  public void OnClicked() {
    if (MyEvent != null) {
      MyEvent();
    }
  }
}

class MyApp {

  static void Main() {
    MyButton btn = new MyButton();
    btn.MyEvent += () => Console.WriteLine("Button Clicked!");
    btn.OnClicked();
  }

}





// ===== video_summary =====
順番があるデータの集合であるListについて見ていきます。


    Listの宣言、初期化
    Count
    要素へのアクセス

// ===== MyApp.cs =====
using System;
using System.Collections.Generic;

// Collection
// - List
// - HashSet
// - Dictionary

class MyApp {

  static void Main() {
    // List<int> scores = new List<int>();
    // scores.Add(30);
    // scores.Add(80);
    // scores.Add(60);
    List<int> scores = new List<int>() { 30, 80 , 60 };
    scores[1] = 100;
    Console.WriteLine(scores.Count);
    foreach (var score in scores) {
      Console.WriteLine(score);
    }
  }

}





// ===== video_summary =====
重複を許さないデータの集合であるHashSetについて見ていきます。


    HashSetの宣言、初期化
    HashSetの扱い

// ===== MyApp.cs =====
using System;
using System.Collections.Generic;

// Collection
// - List
// - HashSet
// - Dictionary

class MyApp {

  static void Main() {
    HashSet<int> answers = new HashSet<int>() { 3, 5, 8, 5 };
    answers.Add(10); // 3, 5, 8, 10
    answers.Remove(3); // 5, 8, 10
    Console.WriteLine(answers.Contains(3)); // false
    foreach (var answer in answers) {
      Console.WriteLine(answer);
    }
  }

}





// ===== video_summary =====
キーと値でデータの集合を管理するDictionaryについて見ていきます。


    Dictionaryの宣言、初期化
    Disctionaryの扱い

// ===== MyApp.cs =====
using System;
using System.Collections.Generic;

// Collection
// - List
// - HashSet
// - Dictionary

class MyApp {

  static void Main() {
    Dictionary<string, int> users = new Dictionary<string, int>() {
      {"taguchi", 50},
      {"fkoji", 80},
    };
    users.Add("dotinstall", 40);
    Console.WriteLine(users["fkoji"]); // 80
    users["taguchi"] = 60;
    foreach (KeyValuePair<string, int> user in users) {
      Console.WriteLine($"{user.Key}: {user.Value}");
    }
  }

}





// ===== video_summary =====
データからなんからの情報を取得するための仕組みであるLINQについて見ていきます。


    SQLのようなLINQへのアクセス
    メソッドのようなLINQへのアクセス

// ===== MyApp.cs =====
using System;
using System.Collections.Generic;
using System.Linq;

// LINQ

class MyApp {

  static void Main() {
    List<double> prices = new List<double>() { 53.2, 48.2, 32.8 };

    // SQL
    // var results = from price in prices
    //   where price * 1.08 > 50.0
    //   select price * 1.08;

    // Method
    var results = prices
      .Select(n => n * 1.08)
      .Where(n => n > 50.0);

    foreach (var result in results) {
      Console.WriteLine(result);
    }

  }

}





