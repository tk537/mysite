
Gitにおけるブランチの概要を、用語とともに説明していきます。

ブランチ
mainブランチ
マージ
HEAD








VS Codeエディタに作業用フォルダを設定したあとに、いくつかコミットを保存していきます。

作業用フォルダの設定
ターミナルの起動
readme.mdの作成
git init
git add readme.md
git commit -m "01を追加"
git commit -am "02を追加"
git log --oneline
補足情報 今回使ったコマンドについて
2023年10月13日
今回使った各コマンドについて理解があいまいな人は以下の動画を復習しておきましょう。

git init / git add / git commit
git commit -am
git log --oneline

readme.md 
# 01
01のテキストです。

# 02
02のテキストです。









ブランチを操作するための基本的なコマンドを見ていきます。

git branch
git branch add-03
git branch
git switch add-03
git branch
git commit -am "03を追加"
git log --oneline

readme.md
# 01
01のテキストです。

# 02
02のテキストです。

# 03
03のテキストです（遅くなってすみません...！）。






mainブランチに切り替えて挙動を確認したあとに、git logで使えるallオプションについて見ていきます。

git switch main
git log --oneline
git log --oneline --all

readme.md 
# 01
01のテキストです。

# 02
02のテキストです。






これまでの作業と、ブランチをマージした際の挙動について図で確認していきます。

これまでの流れ
git merge add-03
マージの挙動
Fast-forwardマージ
no-ffオプション
マージコミット





mainブランチにadd-03ブランチをマージする流れについて見ていきます。

git branch
git merge add-03 --no-ff --no-edit
git log --oneline

readme.md
# 01
01のテキストです。

# 02
02のテキストです。

# 03
03のテキストです。








マージして不要になったブランチを削除する方法について見ていきます。

git branch
git branch -d add-03
git branch
git log --oneline --graph --all

readme.md 
# 01
01のテキストです。

# 02
02のテキストです。

# 03
03のテキストです。





ブランチを分岐する前に戻った上で、新しくブランチを作ってコミットを保存していきます。

git log --oneline --graph
git reset --hard [hash]
git log --oneline
git switch -c add-03
git branch
git commit -am "03を追加"
git log --oneline
補足情報 git resetについて
2023年10月13日
git resetの挙動について忘れてしまった人は以下の動画を復習してみましょう。

・#17 git resetの挙動を理解しよう | Git/GitHub入門 基本操作編
https://dotinstall.com/lessons/basic_gitgithub/63117

readme.md
# 01
01のテキストです。

# 02
02のテキストです。

# 03
03のテキストです





mainブランチにもコミットを保存することで、違った状況でマージするための準備をしていきます。

git switch main
git commit -am "01に補足を追加"
git log --oneline
git log --oneline --graph --all
分岐後に両方のブランチでコミットがある場合の挙動

readme.md
# 01
01のテキストです。
補足のテキストです。

# 02
02のテキストです。




分岐後に両方のブランチにコミットがある場合、fast-forwardではないマージになることを確認します。

コマンド履歴を辿る方法
git log --oneline --graph --all
git merge add-03 --no-edit
git log --oneline --graph --all
git branch -d add-03
git log --oneline --graph --all

readme.md
# 01
01のテキストです。
補足のテキストです。

# 02
02のテキストです。

# 03
03のテキストです。







再度、ブランチの分岐前に戻ったあとに、両方のブランチで同じ箇所を変更してみます。

git log --oneline --graph --all
git reset --hard [hash]
git switch -c add-03
git commit -am "03を追加"
git log --oneline --graph --all
git switch main
git commit -am "03を仮置き"
git log --oneline --graph --all

readme.md
# 01
01のテキストです。

# 02
02のテキストです。

# 03
近日公開予定。









変更内容をGitが判断できないときに発生するコンフリクトと、マージを中断する方法について見ていきます。

git log --oneline --graph --all
git merge add-03 --no-ff --no-edit
git merge --abort

readme.md
# 01
01のテキストです。

# 02
02のテキストです。

# 03
近日公開予定。






マージする際に発生したコンフリクトを解消する方法について見ていきます。

git merge add-03 --no-ff --no-edit
コンフリクトの解消
git commit -a --no-edit
git log --oneline --graph --all
git branch -d add-03
git log --oneline --graph --all

readme.md
# 01
01のテキストです。

# 02
02のテキストです。

# 03
03のテキストです。










再度、ブランチの分岐前に戻ったあとに、ブランチ名を変更する方法、ブランチをマージせずに削除する方法について見ていきます。

git log --oneline --graph --all
git reset --hard [hash]
git log --oneline --graph --all
git switch -c addd-03
git branch -m addd-03 add-03
git branch
git commit -am "03を追加"
git log --oneline --graph --all
git switch main
git branch -D add-03

readme.md
# 01
01のテキストです。

# 02
02のテキストです。




ブランチの分岐元を付け替えることができるgit rebaseの挙動について、図で解説していきます。

git rebase
rebaseの挙動











git rebaseの挙動を確認するために、両方のブランチにコミットを保存していきます。

git log --oneline --graph --all
git switch -c add-03
git commit -am "03を追加"
git log --oneline --graph --all
git switch main
git commit -am "03を仮置き"
git log --oneline --graph --all
git switch add-03
git log --oneline --graph --all

readme.md
# 01
01のテキストです。

# 02
02のテキストです。

# 03
03のテキストです。








git rebaseを使ってブランチの分岐元を、mainの最新のコミットに付け替えていきます。

git log --oneline --graph --all
git rebase main
コンフリクトの解消
git commit -a --no-edit
git rebase --continue
git switch main
git merge add-03 --no-ff --no-edit
git log --oneline --graph --all
git branch -d add-03

readme.md 
# 01
01のテキストです。

# 02
02のテキストです。

# 03
03のテキストです。






    
  </pre>

</body>
</html>