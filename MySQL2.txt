

// ===== video_summary =====
合計や平均など、集計のための関数を扱う方法について見ていきます。


    COUNT()
    SUM()
    AVG()
    MAX()
    MIN()

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes) VALUES
  ('post-1', 12),
  ('post-2', 8),
  ('post-3', 11),
  ('post-4', 3),
  ('post-5', NULL),
  ('post-6', 9),
  ('post-7', 4),
  ('post-8', NULL),
  ('post-9', 31);

-- SELECT COUNT(likes) FROM posts;
-- SELECT COUNT(id) FROM posts;
-- SELECT COUNT(*) FROM posts;

SELECT SUM(likes) FROM posts;
SELECT AVG(likes) FROM posts;
SELECT MAX(likes) FROM posts;
SELECT MIN(likes) FROM posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes)·VALUES¬
··('post-1',·12),¬
··('post-2',·8),¬
··('post-3',·11),¬
··('post-4',·3),¬
··('post-5',·8),¬
··('post-6',·9),¬
··('post-7',·4),¬
··('post-8',·10),¬
··('post-9',·31);¶







// ===== video_summary =====
GROPU BYを使って、エリアごとの合計を集計する方法について見ていきます。


    DISTINCT
    GROUP BY

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

-- SELECT * FROM posts;
-- SELECT DISTINCT area FROM posts;
SELECT area, SUM(likes) FROM posts GROUP BY area;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes)·VALUES¬
··('post-1',·12),¬
··('post-2',·8),¬
··('post-3',·11),¬
··('post-4',·3),¬
··('post-5',·8),¬
··('post-6',·9),¬
··('post-7',·4),¬
··('post-8',·10),¬
··('post-9',·31);¶







// ===== video_summary =====
GROPU BYした結果に抽出条件を設定できるHAVINGについて見ていきます。


    HAVING
    WHEREを使う場合の注意点

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

-- SELECT area, SUM(likes) FROM posts GROUP BY area;

-- SELECT
--   area,
--   SUM(likes)
-- FROM
--   posts
-- GROUP BY
--   area
-- -- WHERE
-- HAVING
--   SUM(likes) > 30;

SELECT
  area,
  SUM(likes)
FROM
  posts
WHERE
  likes > 10
GROUP BY
  area;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¬
¬
SELECT·area,·SUM(likes)·FROM·posts·GROUP·BY·area;¶






// ===== video_summary =====
条件によって違う値を抽出する方法について見ていきます。


    IF()
    CASE

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT
  *,
  IF(likes > 10, 'A', 'B') AS team
FROM
  posts;

SELECT
  *,
  CASE
    WHEN likes > 10 THEN 'A'
    WHEN likes > 5 THEN 'B'
    ELSE 'C'
  END AS team
FROM
  posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¶






// ===== video_summary =====
抽出結果を別テーブルとして切り出す方法を見ていきます。


    CREATE TABLE ... AS ...
    CREATE TABLE ... LIKE ...

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

DROP TABLE IF EXISTS posts_tokyo;
CREATE TABLE posts_tokyo AS SELECT * FROM posts WHERE area = 'Tokyo';

DROP TABLE IF EXISTS posts_copy;
CREATE TABLE posts_copy AS SELECT * FROM posts;

DROP TABLE IF EXISTS posts_skeleton;
CREATE TABLE posts_skeleton LIKE posts;

SHOW TABLES;
SELECT * FROM posts_tokyo;
SELECT * FROM posts_copy;
SELECT * FROM posts_skeleton;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¶






// ===== video_summary =====
抽出結果だけを保持することのできるVIEWについて見ていきます。


    VIEW
    DROP VIEW

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

-- DROP TABLE IF EXISTS posts_tokyo;
-- CREATE TABLE posts_tokyo AS SELECT * FROM posts WHERE area = 'Tokyo';

DROP VIEW IF EXISTS posts_tokyo_view;
CREATE VIEW posts_tokyo_view AS SELECT * FROM posts WHERE area = 'Tokyo';

UPDATE posts SET likes = 15 WHERE id = 1;

SELECT * FROM posts;
-- SELECT * FROM posts_tokyo;
SELECT * FROM posts_tokyo_view;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¬
¬
DROP·TABLE·IF·EXISTS·posts_tokyo;¬
CREATE·TABLE·posts_tokyo·AS·SELECT·*·FROM·posts·WHERE·area·=·'Tokyo';¶






// ===== video_summary =====
複数の抽出結果を一つの結果としてまとめる方法を見ていきます。


    UNION ALL

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

-- SELECT * FROM posts ORDER BY likes DESC;

(SELECT * FROM posts ORDER BY likes DESC LIMIT 3)
UNION ALL
(SELECT * FROM posts ORDER BY likes LIMIT 1);

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¶







// ===== video_summary =====
クエリのなかでクエリを使う方法について見ていきましょう。


    サブクエリ
    サブクエリを使うときの注意点

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

-- SELECT *, AVG(likes) AS avg FROM posts;

SELECT
  *,
  (SELECT AVG(likes) FROM posts) AS avg -- sub query
FROM
  posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¶







// ===== video_summary =====
クエリを関連付けながら実行できる相関サブクエリについて見ていきます。


    相関サブクエリ

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT
  *,
  (SELECT AVG(likes) FROM posts) AS avg,
  (SELECT AVG(likes) FROM posts AS t2 WHERE t1.area = t2.area) AS area_avg
FROM
  posts AS t1;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¬
¬
SELECT¬
··*,¬
··(SELECT·AVG(likes)·FROM·posts)·AS·avg¬
FROM¬
··posts;¶






// ===== video_summary =====
サブクエリを抽出条件として使う方法についてみていきます。


    サブクエリ抽出条件

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT
  *
FROM
  posts
WHERE
  likes = (SELECT MAX(likes) FROM posts);

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¬
¬
SELECT¬
··*,¬
··(SELECT·AVG(likes)·FROM·posts)·AS·avg,¬
··(SELECT·AVG(likes)·FROM·posts·AS·t2·WHERE·t1.area·=·t2.area)·AS·area_avg¬
FROM¬
··posts·AS·t1;¶






// ===== video_summary =====
サブクエリを抽出元のテーブルとして使う方法について見ていきます。


    抽出元としてのサブクエリ

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT area, COUNT(*) AS n FROM posts GROUP BY area;

SELECT
  AVG(n)
FROM
  (SELECT area, COUNT(*) AS n FROM posts GROUP BY area) AS t;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¶









// ===== video_summary =====
PARTITIONやFRAMEを設定して、高度な集計をすることができるウィンドウ関数について見ていきます。


    ウィンドウ関数の説明
    PARTITION
    FRAME

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT area, COUNT(*) AS n FROM posts GROUP BY area;

SELECT
  AVG(n)
FROM
  (SELECT area, COUNT(*) AS n FROM posts GROUP BY area) AS t;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¬
¬
SELECT·area,·COUNT(*)·AS·n·FROM·posts·GROUP·BY·area;¬
¬
SELECT¬
··AVG(n)¬
FROM¬
··(SELECT·area,·COUNT(*)·AS·n·FROM·posts·GROUP·BY·area)·AS·t;¶







// ===== video_summary =====
ウィンドウ関数を使って、PARTITIONを設定する方法について見ていきます。


    OVER ()
    PARTITION BY

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT
  *,
  AVG(likes) OVER () AS avg,
  -- AVG(likes) OVER (PARTITION BY area) AS area_avg,
  -- SUM(likes) OVER (PARTITION BY area) AS area_sum
  AVG(likes) OVER w AS area_avg,
  SUM(likes) OVER w AS area_sum
FROM
  posts
WINDOW
  w AS (PARTITION BY area);

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¬
¬
SELECT·*·FROM·posts;¶







// ===== video_summary =====
PARTITIONのなかで並び替えをしたあとに、FRAMEを設定する方法について見ていきます。


    ORDER BY
    ROWS BETWEEN ... AND ...

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT
  *,
  SUM(likes) OVER (
    PARTITION BY area
    ORDER BY likes
    ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING
  ) AS area_sum
FROM
  posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¬
¬
SELECT¬
··*,¬
··SUM(likes)·OVER·(PARTITION·BY·area)·AS·area_sum¬
FROM¬
··posts;¶







// ===== video_summary =====
OVERを使ったときに使える集計関数について見ていきます。


    ROW_NUMBER()
    RANK()
    DENSE_RANK()

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT
  *,
  ROW_NUMBER() OVER (ORDER BY likes) AS num,
  RANK() OVER (ORDER BY likes) AS rank,
  DENSE_RANK() OVER (ORDER BY likes) AS dense
FROM
  posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¶







// ===== video_summary =====
n個前、n個後の値を求めることができる関数を見ていきます。


    LAG()
    LEAD()

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT
  *,
  -- LAG(likes, 1) OVER (ORDER BY likes) AS lag,
  -- LEAD(likes, 1) OVER (ORDER BY likes) AS lead
  -- LAG(likes) OVER (ORDER BY likes) AS lag,
  -- LEAD(likes) OVER (ORDER BY likes) AS lead
  likes - LAG(likes) OVER (ORDER BY likes) AS diff
FROM
  posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¶





// ===== video_summary =====
一連の処理でデータの整合性を保つことのできるトランザクションについて見ていきます。


    トランザクションの概要

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes, area) VALUES
  ('post-1', 12, 'Tokyo'),
  ('post-2', 8, 'Fukuoka'),
  ('post-3', 11, 'Tokyo'),
  ('post-4', 3, 'Osaka'),
  ('post-5', 8, 'Tokyo'),
  ('post-6', 9, 'Osaka'),
  ('post-7', 4, 'Tokyo'),
  ('post-8', 10, 'Osaka'),
  ('post-9', 31, 'Fukuoka');

SELECT
  *,
  -- LAG(likes, 1) OVER (ORDER BY likes) AS lag,
  -- LEAD(likes, 1) OVER (ORDER BY likes) AS lead
  -- LAG(likes) OVER (ORDER BY likes) AS lag,
  -- LEAD(likes) OVER (ORDER BY likes) AS lead
  likes - LAG(likes) OVER (ORDER BY likes) AS diff
FROM
  posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes,·area)·VALUES¬
··('post-1',·12,·'Tokyo'),¬
··('post-2',·8,·'Fukuoka'),¬
··('post-3',·11,·'Tokyo'),¬
··('post-4',·3,·'Osaka'),¬
··('post-5',·8,·'Tokyo'),¬
··('post-6',·9,·'Osaka'),¬
··('post-7',·4,·'Tokyo'),¬
··('post-8',·10,·'Osaka'),¬
··('post-9',·31,·'Fukuoka');¬
¬
SELECT¬
··*,¬
··--·LAG(likes,·1)·OVER·(ORDER·BY·likes)·AS·lag,¬
··--·LEAD(likes,·1)·OVER·(ORDER·BY·likes)·AS·lead¬
··--·LAG(likes)·OVER·(ORDER·BY·likes)·AS·lag,¬
··--·LEAD(likes)·OVER·(ORDER·BY·likes)·AS·lead¬
··likes·-·LAG(likes)·OVER·(ORDER·BY·likes)·AS·diff¬
FROM¬
··posts;¶







// ===== video_summary =====
トランザクションの挙動をコードで確認していきます。


    START TRANSACTION
    COMMIT
    ROLLBACK

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes) VALUES
  ('post-1', 12),
  ('post-2', 8),
  ('post-3', 11),
  ('post-4', 3),
  ('post-5', 5),
  ('post-6', 9),
  ('post-7', 4),
  ('post-8', 10),
  ('post-9', 31);

START TRANSACTION;
UPDATE posts SET likes = likes - 1 WHERE id = 1;
-- UPDATE posts SET likes = likes + 1 WHERE id = 2;
-- COMMIT;
ROLLBACK;

SELECT * FROM posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes)·VALUES¬
··('post-1',·12),¬
··('post-2',·8),¬
··('post-3',·11),¬
··('post-4',·3),¬
··('post-5',·5),¬
··('post-6',·9),¬
··('post-7',·4),¬
··('post-8',·10),¬
··('post-9',·31);¶







// ===== video_summary =====
テーブルを分割してデータを管理しやすくする方法について見ていきます。


    テーブルの分割
    正規化

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  PRIMARY KEY (id)
);

INSERT INTO posts (message, likes) VALUES
  ('post-1', 12),
  ('post-2', 8),
  ('post-3', 11),
  ('post-4', 3),
  ('post-5', 5),
  ('post-6', 9),
  ('post-7', 4),
  ('post-8', 10),
  ('post-9', 31);

START TRANSACTION;
UPDATE posts SET likes = likes - 1 WHERE id = 1;
-- UPDATE posts SET likes = likes + 1 WHERE id = 2;
-- COMMIT;
ROLLBACK;

SELECT * FROM posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message,·likes)·VALUES¬
··('post-1',·12),¬
··('post-2',·8),¬
··('post-3',·11),¬
··('post-4',·3),¬
··('post-5',·5),¬
··('post-6',·9),¬
··('post-7',·4),¬
··('post-8',·10),¬
··('post-9',·31);¬
¬
START·TRANSACTION;¬
UPDATE·posts·SET·likes·=·likes·-·1·WHERE·id·=·1;¬
--·UPDATE·posts·SET·likes·=·likes·+·1·WHERE·id·=·2;¬
--·COMMIT;¬
ROLLBACK;¬
¬
SELECT·*·FROM·posts;¶






// ===== video_summary =====
テーブルを分割してcommentsテーブルを作成する方法について見ていきます。


    commentsテーブルの作成

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140), 
  PRIMARY KEY (id)
);

DROP TABLE IF EXISTS comments;
CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140), 
  PRIMARY KEY (id)
);

INSERT INTO posts (message) VALUES 
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment) VALUES 
  (1, 'comment-1-1'),
  (1, 'comment-1-2'),
  (3, 'comment-3-1'),
  (4, 'comment-4-1');

SELECT * FROM posts;
SELECT * FROM comments;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¶






// ===== video_summary =====
2つのテーブルからデータを取得する方法について見ていきます。


    内部結合の説明
    左外部結合の説明
    右外部結合の説明

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140), 
  PRIMARY KEY (id)
);

DROP TABLE IF EXISTS comments;
CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140), 
  PRIMARY KEY (id)
);

INSERT INTO posts (message) VALUES 
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment) VALUES 
  (1, 'comment-1-1'),
  (1, 'comment-1-2'),
  (3, 'comment-3-1'),
  (4, 'comment-4-1');

SELECT * FROM posts;
SELECT * FROM comments;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),·¬
··PRIMARY·KEY·(id)¬
);¬
¬
DROP·TABLE·IF·EXISTS·comments;¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),·¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES·¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment)·VALUES·¬
··(1,·'comment-1-1'),¬
··(1,·'comment-1-2'),¬
··(3,·'comment-3-1'),¬
··(4,·'comment-4-1');¬
¬
SELECT·*·FROM·posts;¬
SELECT·*·FROM·comments;¶







// ===== video_summary =====
コメントがついてる投稿だけ抜き出す方法について見ていきます。


    INNER JOIN

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

DROP TABLE IF EXISTS comments;
CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  PRIMARY KEY (id)
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment) VALUES
  (1, 'comment-1-1'),
  (1, 'comment-1-2'),
  (3, 'comment-3-1'),
  (4, 'comment-4-1');

SELECT
  -- *
  -- posts.id, posts.message, comments.comment
  posts.id, message, comment
FROM
  -- posts INNER JOIN comments ON posts.id = comments.post_id;
  posts JOIN comments ON posts.id = comments.post_id;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
DROP·TABLE·IF·EXISTS·comments;¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment)·VALUES¬
··(1,·'comment-1-1'),¬
··(1,·'comment-1-2'),¬
··(3,·'comment-3-1'),¬
··(4,·'comment-4-1');¬
¬
SELECT·*·FROM·posts;¬
SELECT·*·FROM·comments;¶







// ===== video_summary =====
左外部結合と右外部結合について見ていきます。


    LEFT OUTER JOIN
    RIGHT OUTER JOIN

// ===== main.sql =====
DROP TABLE IF EXISTS posts;
CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

DROP TABLE IF EXISTS comments;
CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  PRIMARY KEY (id)
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment) VALUES
  (1, 'comment-1-1'),
  (1, 'comment-1-2'),
  (3, 'comment-3-1'),
  (4, 'comment-4-1');

SELECT
  *
FROM
  posts JOIN comments ON posts.id = comments.post_id;

SELECT
  *
FROM
  -- posts LEFT OUTER JOIN comments ON posts.id = comments.post_id;
  posts LEFT JOIN comments ON posts.id = comments.post_id;

SELECT
  *
FROM
  posts RIGHT JOIN comments ON posts.id = comments.post_id;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
DROP·TABLE·IF·EXISTS·comments;¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment)·VALUES¬
··(1,·'comment-1-1'),¬
··(1,·'comment-1-2'),¬
··(3,·'comment-3-1'),¬
··(4,·'comment-4-1');¬
¬
SELECT¬
··*¬
FROM¬
··posts·JOIN·comments·ON·posts.id·=·comments.post_id;¶







// ===== video_summary =====
データの整合性を取るために、外部キー制約について見ていきます。


    外部キー制約の設定

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  PRIMARY KEY (id),
  FOREIGN KEY (post_id) REFERENCES posts(id)
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment) VALUES
  (1, 'comment-1-1'),
  (1, 'comment-1-2'),
  (3, 'comment-3-1');
  -- (4, 'comment-4-1');

SELECT * FROM posts;
SELECT * FROM comments;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
DROP·TABLE·IF·EXISTS·comments;¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment)·VALUES¬
··(1,·'comment-1-1'),¬
··(1,·'comment-1-2'),¬
··(3,·'comment-3-1'),¬
··(4,·'comment-4-1');¶







// ===== video_summary =====
外部キーの設定と合わせて、データの整合性を取るための方法を見ていきます。


    ON DELETE CASCADE
    ON UPDATE CASCADE

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  PRIMARY KEY (id),
  FOREIGN KEY (post_id) REFERENCES posts(id)
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment) VALUES
  (1, 'comment-1-1'),
  (1, 'comment-1-2'),
  (3, 'comment-3-1');

DELETE FROM posts WHERE id = 3;
UPDATE posts SET id = 100 WHERE id = 1;

SELECT * FROM posts;
SELECT * FROM comments;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··PRIMARY·KEY·(id),¬
··FOREIGN·KEY·(post_id)·REFERENCES·posts(id)¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment)·VALUES¬
··(1,·'comment-1-1'),¬
··(1,·'comment-1-2'),¬
··(3,·'comment-3-1');¬
¬
SELECT·*·FROM·posts;¬
SELECT·*·FROM·comments;¶







// ===== video_summary =====
直前に挿入されたレコードのIDを調べる命令についてみていきます。


    LAST_INSERT_ID()

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  PRIMARY KEY (id),
  FOREIGN KEY (post_id) REFERENCES posts(id)
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment) VALUES
  (1, 'comment-1-1'),
  (1, 'comment-1-2'),
  (3, 'comment-3-1');

INSERT INTO posts (message) VALUES
  ('new post!');

INSERT INTO comments (post_id, comment) VALUES
  (LAST_INSERT_ID(), 'new comment');

SELECT * FROM posts;
SELECT * FROM comments;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··PRIMARY·KEY·(id),¬
··FOREIGN·KEY·(post_id)·REFERENCES·posts(id)¬
····ON·DELETE·CASCADE¬
····ON·UPDATE·CASCADE¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment)·VALUES¬
··(1,·'comment-1-1'),¬
··(1,·'comment-1-2'),¬
··(3,·'comment-3-1');¬
¬
SELECT·*·FROM·posts;¬
SELECT·*·FROM·comments;¶





// ===== video_summary =====
コメントにコメントを紐付ける方法について見ていきます。


    parent_idの設定

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  parent_id INT,
  PRIMARY KEY (id),
  FOREIGN KEY (post_id) REFERENCES posts(id)
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment, parent_id) VALUES
  (1, 'comment-1-1', NULL),
  (1, 'comment-1-2', NULL),
  (3, 'comment-3-1', NULL),
  (1, 'comment-1-2-1', 2),
  (1, 'comment-1-2-2', 2),
  (1, 'comment-1-2-1-1', 4);

/*
post-1
  comment-1-1
  comment-1-2
    comment-1-2-1
      comment-1-2-1-1
    comment-1-2-2
post-2
post-3
  comment-3-1
*/

SELECT * FROM posts;
SELECT * FROM comments;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··PRIMARY·KEY·(id),¬
··FOREIGN·KEY·(post_id)·REFERENCES·posts(id)¬
····ON·DELETE·CASCADE¬
····ON·UPDATE·CASCADE¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment)·VALUES¬
··(1,·'comment-1-1'),¬
··(1,·'comment-1-2'),¬
··(3,·'comment-3-1');¬
¬
SELECT·*·FROM·posts;¬
SELECT·*·FROM·comments;¶






// ===== video_summary =====
コメントのコメントを抽出してUNION ALLでまとめる方法について見ていきます。


    コメントのコメントを抽出
    UNION ALL

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  parent_id INT,
  PRIMARY KEY (id),
  FOREIGN KEY (post_id) REFERENCES posts(id)
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment, parent_id) VALUES
  (1, 'comment-1-1', NULL),
  (1, 'comment-1-2', NULL),
  (3, 'comment-3-1', NULL),
  (1, 'comment-1-2-1', 2),
  (1, 'comment-1-2-2', 2),
  (1, 'comment-1-2-1-1', 4);

/*
post-1
  comment-1-1
  comment-1-2
    comment-1-2-1
      comment-1-2-1-1
    comment-1-2-2
post-2
post-3
  comment-3-1
*/

SELECT * FROM posts;
SELECT * FROM comments;

SELECT * FROM comments WHERE parent_id = 2
UNION ALL
SELECT
  comments.*
FROM
  comments JOIN (
    SELECT * FROM comments WHERE parent_id = 2
  ) AS t
ON
  comments.parent_id = t.id;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··parent_id·INT,¬
··PRIMARY·KEY·(id),¬
··FOREIGN·KEY·(post_id)·REFERENCES·posts(id)¬
····ON·DELETE·CASCADE¬
····ON·UPDATE·CASCADE¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment,·parent_id)·VALUES¬
··(1,·'comment-1-1',·NULL),¬
··(1,·'comment-1-2',·NULL),¬
··(3,·'comment-3-1',·NULL),¬
··(1,·'comment-1-2-1',·2),¬
··(1,·'comment-1-2-2',·2),¬
··(1,·'comment-1-2-1-1',·4);¬
¬
/*¬






// ===== video_summary =====
クエリ内で使える一時的なテーブルを設定できるCTEについて見ていきます。


    CTEの概要
    再帰的ではないCTE

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  parent_id INT,
  PRIMARY KEY (id),
  FOREIGN KEY (post_id) REFERENCES posts(id)
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment, parent_id) VALUES
  (1, 'comment-1-1', NULL),
  (1, 'comment-1-2', NULL),
  (3, 'comment-3-1', NULL),
  (1, 'comment-1-2-1', 2),
  (1, 'comment-1-2-2', 2),
  (1, 'comment-1-2-1-1', 4);

/*
post-1
  comment-1-1
  comment-1-2
    comment-1-2-1
      comment-1-2-1-1
    comment-1-2-2
post-2
post-3
  comment-3-1
*/

SELECT * FROM posts;
SELECT * FROM comments;

SELECT * FROM comments WHERE parent_id = 2
UNION ALL
SELECT
  comments.*
FROM
  comments JOIN (
    SELECT * FROM comments WHERE parent_id = 2
  ) AS t
ON
  comments.parent_id = t.id;

-- CTE (Common Table Expression)
--   再帰的なCTE
--   再帰的ではないCTE

WITH t AS (
  SELECT * FROM comments WHERE parent_id = 2
)
SELECT
  comments.*
FROM
  comments JOIN t
ON
  comments.parent_id = t.id;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··parent_id·INT,¬
··PRIMARY·KEY·(id),¬
··FOREIGN·KEY·(post_id)·REFERENCES·posts(id)¬
····ON·DELETE·CASCADE¬
····ON·UPDATE·CASCADE¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment,·parent_id)·VALUES¬
··(1,·'comment-1-1',·NULL),¬
··(1,·'comment-1-2',·NULL),¬
··(3,·'comment-3-1',·NULL),¬
··(1,·'comment-1-2-1',·2),¬
··(1,·'comment-1-2-2',·2),¬
··(1,·'comment-1-2-1-1',·4);¬
¬
/*¬






// ===== video_summary =====
階層が深いデータを処理するために再帰的なCTEを使ってみます。


    WITH RECURSIVE ... AS ...

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  parent_id INT,
  PRIMARY KEY (id),
  FOREIGN KEY (post_id) REFERENCES posts(id)
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment, parent_id) VALUES
  (1, 'comment-1-1', NULL),
  (1, 'comment-1-2', NULL),
  (3, 'comment-3-1', NULL),
  (1, 'comment-1-2-1', 2),
  (1, 'comment-1-2-2', 2),
  (1, 'comment-1-2-1-1', 4);

/*
post-1
  comment-1-1
  comment-1-2
    comment-1-2-1
      comment-1-2-1-1
    comment-1-2-2
post-2
post-3
  comment-3-1
*/

SELECT * FROM posts;
SELECT * FROM comments;

-- CTE (Common Table Expression)
--   再帰的なCTE
--   再帰的ではないCTE

WITH RECURSIVE t AS (
  -- n = 1
  SELECT * FROM comments WHERE parent_id = 2
  UNION ALL
  -- n >= 2
  SELECT
    comments.*
  FROM
    comments JOIN t
  ON
    comments.parent_id = t.id
)
SELECT * FROM t;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··parent_id·INT,¬
··PRIMARY·KEY·(id),¬
··FOREIGN·KEY·(post_id)·REFERENCES·posts(id)¬
····ON·DELETE·CASCADE¬
····ON·UPDATE·CASCADE¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment,·parent_id)·VALUES¬
··(1,·'comment-1-1',·NULL),¬
··(1,·'comment-1-2',·NULL),¬
··(3,·'comment-3-1',·NULL),¬
··(1,·'comment-1-2-1',·2),¬
··(1,·'comment-1-2-2',·2),¬
··(1,·'comment-1-2-1-1',·4);¬
¬
/*¬








// ===== video_summary =====
再帰的なCTEをつかって階層が深いコメントを抽出していきます。


    再帰的なCTEの動作確認

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE comments (
  id INT NOT NULL AUTO_INCREMENT,
  post_id INT,
  comment VARCHAR(140),
  parent_id INT,
  PRIMARY KEY (id),
  FOREIGN KEY (post_id) REFERENCES posts(id)
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

INSERT INTO comments (post_id, comment, parent_id) VALUES
  (1, 'comment-1-1', NULL),
  (1, 'comment-1-2', NULL),
  (3, 'comment-3-1', NULL),
  (1, 'comment-1-2-1', 2),
  (1, 'comment-1-2-2', 2),
  (1, 'comment-1-2-1-1', 4),
  (1, 'new comment', 6);

/*
post-1
  comment-1-1
  comment-1-2
    comment-1-2-1
      comment-1-2-1-1
    comment-1-2-2
post-2
post-3
  comment-3-1
*/

SELECT * FROM posts;
SELECT * FROM comments;

-- CTE (Common Table Expression)
--   再帰的なCTE
--   再帰的ではないCTE

WITH RECURSIVE t AS (
  -- n = 1
  SELECT * FROM comments WHERE parent_id = 2
  UNION ALL
  -- n >= 2
  SELECT
    comments.*
  FROM
    comments JOIN t
  ON
    comments.parent_id = t.id
)
SELECT * FROM t;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TABLE·comments·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··post_id·INT,¬
··comment·VARCHAR(140),¬
··parent_id·INT,¬
··PRIMARY·KEY·(id),¬
··FOREIGN·KEY·(post_id)·REFERENCES·posts(id)¬
····ON·DELETE·CASCADE¬
····ON·UPDATE·CASCADE¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
INSERT·INTO·comments·(post_id,·comment,·parent_id)·VALUES¬
··(1,·'comment-1-1',·NULL),¬
··(1,·'comment-1-2',·NULL),¬
··(3,·'comment-3-1',·NULL),¬
··(1,·'comment-1-2-1',·2),¬
··(1,·'comment-1-2-2',·2),¬
··(1,·'comment-1-2-1-1',·4);¬
¬
/*¬





// ===== video_summary =====
あるテーブルで何らかの変更がおきたときに、それをトリガーにして処理する事ができるTRIGGERについて見ていきます。


    TRIGGER
    DROP TRIGGER

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;
DROP TABLE IF EXISTS logs;
DROP TRIGGER IF EXISTS posts_update_trigger;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE logs (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  created DATETIME DEFAULT NOW(),
  PRIMARY KEY (id)
);

CREATE TRIGGER
  posts_update_trigger
AFTER UPDATE ON
  posts
FOR EACH ROW
  INSERT INTO
    logs (message)
  VALUES
    ('Updated');

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');

SELECT * FROM posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
SELECT·*·FROM·posts;¶







// ===== video_summary =====
TRIGGERの動作確認をしたあとに、更新前後の値を取得する方法についても見ていきます。


    OLD, NEW
    SHOW TRIGGERS
    \G

// ===== main.sql =====
DROP TABLE IF EXISTS comments;
DROP TABLE IF EXISTS posts;
DROP TABLE IF EXISTS logs;
DROP TRIGGER IF EXISTS posts_update_trigger;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  PRIMARY KEY (id)
);

CREATE TABLE logs (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  created DATETIME DEFAULT NOW(),
  PRIMARY KEY (id)
);

CREATE TRIGGER
  posts_update_trigger
AFTER UPDATE ON
  posts
FOR EACH ROW
  INSERT INTO
    logs (message)
  VALUES
    -- ('Updated');
    (CONCAT(OLD.message, ' -> ', NEW.message));

INSERT INTO posts (message) VALUES
  ('post-1'),
  ('post-2'),
  ('post-3');
UPDATE posts SET message = 'post-1 updated' WHERE id = 1;

SELECT * FROM posts;
SELECT * FROM logs;

-- SHOW TRIGGERS;
SHOW TRIGGERS\G

// ===== unknown =====
DROP·TABLE·IF·EXISTS·comments;¬
DROP·TABLE·IF·EXISTS·posts;¬
DROP·TABLE·IF·EXISTS·logs;¬
DROP·TRIGGER·IF·EXISTS·posts_update_trigger;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TABLE·logs·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··created·DATETIME·DEFAULT·NOW(),¬
··PRIMARY·KEY·(id)¬
);¬
¬
CREATE·TRIGGER¬
··posts_update_trigger¬
AFTER·UPDATE·ON¬
··posts¬
FOR·EACH·ROW¬
··INSERT·INTO¬
····logs·(message)¬
··VALUES¬
····('Updated');¬
¬
INSERT·INTO·posts·(message)·VALUES¬
··('post-1'),¬
··('post-2'),¬
··('post-3');¬
¬
SELECT·*·FROM·posts;¶






// ===== video_summary =====
別のファイルからデータを読み込む方法について見ていきます。


    LOAD DATA LOCAL INFILE
    FIELDS TERMINATED
    LINES TERMINATED
    IGNORE n LINES

// ===== main.sql =====
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

LOAD DATA LOCAL INFILE 'data.csv' INTO TABLE posts
  FIELDS TERMINATED BY ','
  LINES TERMINATED BY '\n'
  IGNORE 1 LINES
  (message, likes, area);

SELECT * FROM posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¶

// ===== unknown =====







// ===== video_summary =====
検索を高速化することができるインデックスについて見ていきます。


    インデックスの概要
    インデックス設定時の注意点

// ===== main.sql =====
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

LOAD DATA LOCAL INFILE 'data.csv' INTO TABLE posts
  FIELDS TERMINATED BY ','
  LINES TERMINATED BY '\n'
  IGNORE 1 LINES
  (message, likes, area);

SELECT * FROM posts;

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
LOAD·DATA·LOCAL·INFILE·'data.csv'·INTO·TABLE·posts¬
··FIELDS·TERMINATED·BY·','¬
··LINES·TERMINATED·BY·'\n'¬
··IGNORE·1·LINES¬
··(message,·likes,·area);¬
¬
SELECT·*·FROM·posts;¶

// ===== unknown =====







// ===== video_summary =====
設定されているインデックスを確認したあとにEXPLAINの使い方について見ていきます。


    SHOW INDEX
    EXPLAIN

// ===== main.sql =====
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

LOAD DATA LOCAL INFILE 'data.csv' INTO TABLE posts
  FIELDS TERMINATED BY ','
  LINES TERMINATED BY '\n'
  IGNORE 1 LINES
  (message, likes, area);

SHOW INDEX FROM posts\G
EXPLAIN SELECT * FROM posts WHERE id = 30\G

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
LOAD·DATA·LOCAL·INFILE·'data.csv'·INTO·TABLE·posts¬
··FIELDS·TERMINATED·BY·','¬
··LINES·TERMINATED·BY·'\n'¬
··IGNORE·1·LINES¬
··(message,·likes,·area);¶

// ===== unknown =====







// ===== video_summary =====
インデックスを設定する方法と、外す方法について見ていきます。


    ALTER TABLE ... ADD INDEX ...
    ALTER TABLE ... DROP INDEX ...

// ===== main.sql =====
DROP TABLE IF EXISTS posts;

CREATE TABLE posts (
  id INT NOT NULL AUTO_INCREMENT,
  message VARCHAR(140),
  likes INT,
  area VARCHAR(20),
  PRIMARY KEY (id)
);

LOAD DATA LOCAL INFILE 'data.csv' INTO TABLE posts
  FIELDS TERMINATED BY ','
  LINES TERMINATED BY '\n'
  IGNORE 1 LINES
  (message, likes, area);

-- SHOW INDEX FROM posts\G
-- EXPLAIN SELECT * FROM posts WHERE id = 30\G

ALTER TABLE posts ADD INDEX index_area(area);
-- SHOW INDEX FROM posts\G
-- EXPLAIN SELECT * FROM posts WHERE area = 'Kyoto'\G

ALTER TABLE posts DROP INDEX index_area;
SHOW INDEX FROM posts\G

// ===== unknown =====
DROP·TABLE·IF·EXISTS·posts;¬
¬
CREATE·TABLE·posts·(¬
··id·INT·NOT·NULL·AUTO_INCREMENT,¬
··message·VARCHAR(140),¬
··likes·INT,¬
··area·VARCHAR(20),¬
··PRIMARY·KEY·(id)¬
);¬
¬
LOAD·DATA·LOCAL·INFILE·'data.csv'·INTO·TABLE·posts¬
··FIELDS·TERMINATED·BY·','¬
··LINES·TERMINATED·BY·'\n'¬
··IGNORE·1·LINES¬
··(message,·likes,·area);¬
¬
SHOW·INDEX·FROM·posts\G¬
EXPLAIN·SELECT·*·FROM·posts·WHERE·id·=·30\G¶

// ===== unknown =====







