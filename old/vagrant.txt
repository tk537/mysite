<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <pre>

$ vagrant init hashicorp/precise64
$ vagrant up


仮想マシン

1. Box（テンプレート）を取得

2. 仮想マシンを初期化

3. 仮想マシンを起動
vagrant box add centos64 http://developer.nrel.gov/downloads/vagrant-boxes/CentOS-6.4-x86_64-v20130427.box
vagrant box list
vagrant box -h
ls ~/.vagrant.d/boxes


仮想マシン

1. Box（テンプレート）を取得

2. 仮想マシンを初期化

3. 仮想マシンを起動
mkdir myCentOSVM
cd myCentOSVM
vagrant init centos64
ls
less Vagrantfile
vagrant up


仮想マシン

1. Box（テンプレート）を取得

2. 仮想マシンを初期化

3. 仮想マシンを起動


- status
- suspend - resume
- halt - up
- reload

- destroy
vagrant status
vagrant suspend
vagrant resume



vagrant destroy仮想マシンにSSH接続し、Webサーバーを立ち上げていきます。

vagrant ssh
pwd
sudo yum -y install httpd
sudo service httpd start
sudo chkconfig httpd on
sudo service iptables stop
sudo chkconfig iptables off



仮想マシン内にindex.htmlを作り、ネットワークの設定をしたあとにブラウザから確認してみます。

cd /var/www/html
sudo vi index.html
cat index.html
Vagrantfileの編集（config.vm.network :private_network, ip: "192.168.33.10"）
vagrant reload


共有フォルダを設定し、ホストOSから仮想マシンのファイルを編集する方法について見ていき 

Sandboxモードを使うことができるsaharaプラグインについて学びます。
vagrant sandbox on
vagrant sandbox status
vagrant sandbox rollback
vagrant sandbox commit
vagrant sandbox off


仮想マシンが立ち上がったあとに実行される処理をProvisioningで指定していきます。

Vagrantfileの編集
config.vm.provision :shell, :inline => "echo hello world"
動作確認




シェルスクリプトを記述したファイルをProvisioningとして読み込む方法について見ていきます。

Vagrantfileの編集
config.vm.provision :shell, :path => "provision.sh"
vagrant provision
動作確認

sudo yum -y install httpd
sudo service httpd start
sudo chkconfig httpd on





現在の仮想マシンの状態からBoxを作っていく方法について見ていきます。またそのBoxをもとに仮想マシンを作り、動作確認も行います。

vagrant package
vagrant box add my_box package.box
ls ~/.vagrant.d/boxes
rm package.box
cd ..
mkdir myBox
cd myBox
vagrant init my_box
vagrant up
vagrant ssh
sudo service httpd status


Vagrantに便利な機能を提供するpluginの導入方法などについて見ていきます。

vagrant plugin install sahara
vagrant plugin -h
vagrant plugin list
vagrant plugin uninstall sahara
vagrant plugin list




Sandboxモードを使うことができるsaharaプラグインについて学びます。

vagrant sandbox on
vagrant sandbox status
vagrant sandbox rollback
vagrant sandbox commit
vagrant sandbox off
  </pre>
</body>
</html>