// ===== video_summary =====
まずは概要を説明したのちに、公式サイト、必要となる知識、レッスンにおける環境について説明していきます。


    概要
    公式サイト
    必要となる知識
    レッスンにおける環境
    activerecordのインストール

// ===== commentBox 1 =====
補足情報
インストールの際にはバージョンを指定するようにしてください




2019年9月9日





動画通りにコマンドを実行してしまうと最新版の Active Record や SQLite をインストールしようとしてしまい、ローカル開発環境ではエラーになってしまいます。
gem install activerecord -v 5.0.1 --no-doc --no-ri
gem install sqlite3 -v 1.3.13 --no-doc --no-ri
のようにバージョンを指定してインストールするようにしてください。

// ===== commentBox 2 =====
補足情報
Active Recordsの実行環境について




2020年12月2日





現在ローカル開発環境のレッスンどおりに環境を構築することができません（レッスン内で利用しているCentOS6が終了したため）。実行環境の代替手段についてはローカル開発環境を構築できないのですが……をご覧ください。

// ===== commentBox 3 =====
補足情報
Rubyのバージョンアップをする方法




2017年8月19日





ドットインストールのレッスンで構築したローカル開発環境ではRubyのバージョン管理にrbenvを使っています。
もしバージョンアップをしたいなら、以下の手順をお試しください（Rubyのバージョンを2.4.1.にしたい場合）。

$ cd ~/.rbenv
$ git pull origin master
$ cd ~/.rbenv/plugins/ruby-build 
$ git pull origin master
$ rbenv install 2.4.1
$ rbenv global 2.4.1
$ rbenv rehash
$ ruby -v


上記のように作業をしてみてきちんとバージョンが上がっていれば作業完了です。]

// ===== commentBox 4 =====
補足情報
必要となる知識について




2016年12月29日





今回のレッスンについては以下も参考にしてみてください。

・Ruby入門 (全26回)
http://dotinstall.com/lessons/basic_ruby_v3
・SQLite入門 (全30回)
http://dotinstall.com/lessons/basic_sqlite_v2

// ===== read.me =====
Active Record

# 概要
- Ruby on Rails
- OR Mapper (Object <> RDB)

# 公式サイト

# 知識

# 環境
- ローカル開発環境


// ===== video_summary =====
今回Active Recordで扱っていくデータベースを作っていきます。


    import.sqlの作成
    データベースの作成

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);


// ===== video_summary =====
RubyからActive Recordを扱っていくためのさまざまな設定をしていきます。


    Active Recordの設定
    タイムゾーンの設定
    データベースの設定
    クラスの設定

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);



// ===== video_summary =====
RubyからActive Recordを扱っていくためのさまざまな設定をしていきます。


    Active Recordの設定
    タイムゾーンの設定
    データベースの設定
    クラスの設定

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);


// ===== video_summary =====
Active Recordを使ってレコードを挿入していくさまざまな方法について見ていきます。


    レコードの挿入方法

// ===== commentBox 1 =====
補足情報
undefined method `find_zone!' for Time:Class (NoMethodError) と出る場合




2018年5月1日





最新版の ActiveRecord では、


  main.rb:4:in `<main>': undefined method `find_zone!' for Time:Class (NoMethodError)


というエラーが発生してしまいます。エラーを回避するには main.rb の先頭に、

require 'active_support/all'


を追加してください。

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

# insert

user = User.new
user.name = "tanaka"
user.age = 23
user.save

# user = User.new(:name => "hayashi", :age => 25)
user = User.new(name: "hayashi", age: 25)
user.save

User.create(name: "hoshi", age: 22)

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);



// ===== video_summary =====
レコードの挿入方法を紹介したのちに、それがSQLとしてどのように発行されているかを確認していきます。


    レコードの挿入方法
    Loggerの設定
    SQLの確認

// ===== main.rb =====
require 'active_record'
require 'pp'
require 'logger'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.logger = Logger.new(STDOUT)

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

# insert

user = User.new do |u|
  u.name = "mochizuki"
  u.age = 18
end
user.save

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);


// ===== video_summary =====
レコードを抽出する基本的な方法について見ていきます。


    レコードの抽出

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# pp User.all
# pp User.select("id, name, age").all

# pp User.select("id, name, age").first
# pp User.select("id, name, age").last

pp User.select("id, name, age").first(3)

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);


// ===== video_summary =====
主キーやフィールドを指定してレコードを抽出する方法について見ていきます。


    主キーによる抽出
    フィールドによる抽出

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# pp User.select("id, name, age").find(3)
# pp User.select("id, name, age").find_by(name: "tanaka")
# pp User.select("id, name, age").find_by name: "tanaka"
# pp User.select("id, name, age").find_by_name "tanaka"
# pp User.select("id, name, age").find_by_name("tanaka")

# pp User.select("id, name, age").find_by_name("kiriya")
pp User.select("id, name, age").find_by_name!("kiriya")

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);


// ===== video_summary =====
whereを使ってレコードを抽出するさまざまな方法について解説していきます。


    where
    AND検索
    OR検索
    NOT検索

// ===== import.sql =====
drop table if exists users;
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);

drop table if exists comments;
create table comments (
  id integer primary key,
  user_id integer,
  body text,
  created_at,
  updated_at
);

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# where

# pp User.select("id, name, age").where(age: 20..29)
# pp User.select("id, name, age").where(age: [19, 31])

# AND

# pp User.select("id, name, age").where("age >= 20").where("age < 30")
# pp User.select("id, name, age").where("age >= 20 and age < 30")

# OR

# pp User.select("id, name, age").where("age <= 20 or age >= 30")
# pp User.select("id, name, age").where("age <= 20").or(User.select("id, name, age").where("age >= 30"))
# pp User.where("age <= 20").or(User.where("age >= 30")).select("id, name, age")

# NOT

pp User.select("id, name, age").where.not(id: 3)



// ===== video_summary =====
プレースホルダーを使って条件文字列に安全に値を埋め込む方法を見ていきます。


    プレースホルダー

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

min = 20
max = 30

# pp User.select("id, name, age").where("age >= #{min} and age < #{max}") # NG!!!

# pp User.select("id, name, age").where("age >= ? and age < ?", min, max)
# pp User.select("id, name, age").where("age >= :min and age < :max", {min: min, max: max})

pp User.select("id, name, age").where("name like ?", "%i")

// ===== import.sql =====
drop table if exists users;
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);

drop table if exists comments;
create table comments (
  id integer primary key,
  user_id integer,
  body text,
  created_at,
  updated_at
);


// ===== video_summary =====
並べ替えや抽出件数を制限する方法について見ていきます。


    並べ替え
    抽出件数の制限

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# order
# pp User.select("id, name, age").order("age")
# pp User.select("id, name, age").order(:age)
# pp User.select("id, name, age").order("age desc")
# pp User.select("id, name, age").order(age: :desc)

# limit
# pp User.select("id, name, age").order(:age).limit(3)
pp User.select("id, name, age").order(:age).limit(3).offset(1)



// ===== video_summary =====
scopeやクラスメソッドを使って抽出条件を登録する方法について見ていきます。


    scope
    クラスメソッド
    引数の利用

// ===== import.sql =====
drop table if exists users;
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);

drop table if exists comments;
create table comments (
  id integer primary key,
  user_id integer,
  body text,
  created_at,
  updated_at
);

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
  # class method
  # def self.top3
  #   select("id, name, age").order(:age).limit(3)
  # end
  # scope
  # scope :top3, -> { select("id, name, age").order(:age).limit(3) }

  # class method
  # def self.top(num)
  #   select("id, name, age").order(:age).limit(num)
  # end
  # scope
  scope :top, ->(num) { select("id, name, age").order(:age).limit(num) }
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# pp User.select("id, name, age").order(:age).limit(3)
# pp User.top3
pp User.top(2)



// ===== video_summary =====
検索して存在していなかったらレコードを挿入する、という方法について見ていきます。


    find_or_create_by

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# find_or_create_by

# user = User.find_or_create_by(name: "hayashi")
# pp user

# user = User.find_or_create_by(name: "yokota")
# pp user
user = User.find_or_create_by(name: "yokota") do |u|
  u.age = 18
end
pp user



// ===== video_summary =====
レコードの内容を更新するための方法について見ていきます。


    update
    update_all

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# update

# User.update(1, age: 50)
# User.where(name: "tanaka").update(age: 60)
# User.where(name: "tanaka").update(age: 70, name: "taguchi")
# User.where("age >= 20").update(age: 80)

# update_all

User.where("age >= 20").update_all("age = age + 2")

pp User.select("id, name, age").all

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);



// ===== video_summary =====
レコードを削除するための命令について解説していきます。


    delete
    destroy

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# delete: 単機能だけど高速
# - delete
# - delete_all
# destroy: 高機能だけど低速
# - destroy
# - destroy_all

# User.delete(1)
User.where("age >= 25").delete_all
pp User.select("id, name, age").all

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);



// ===== video_summary =====
データを作成、更新する際にルールを定めることができるValidationについて見ていきます。


    Validation
    エラーメッセージの抽出

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
  # validates :name, presence: true
  # validates :age, presence: true
  validates :name, :age, presence: true
  validates :name, length: { minimum: 3 }
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# Validation

user = User.new(name: nil, age: nil)
# user.save!

if !user.save
  pp user.errors.messages
end

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);



// ===== video_summary =====
特定のタイミングで自動処理を行うためのCallbackについて見ていきます。


    Callback

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

class User < ActiveRecord::Base
  before_destroy :print_before_msg
  after_destroy :print_after_msg

  protected
    def print_before_msg
      puts "#{self.name} will be deleted"
    end
    def print_after_msg
      puts "#{self.name} deleted"
    end
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

# callback

# destroy
# before_destroy
# after_destroy

User.where("age >= 20").destroy_all

// ===== import.sql =====
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);



// ===== video_summary =====
複数のオブジェクトを関連付けて扱うためのAssociationについて見ていきます。


    テーブルの作成
    Associationの設定

// ===== import.sql =====
drop table if exists users;
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);

drop table if exists comments;
create table comments (
  id integer primary key,
  user_id integer,
  body text,
  created_at,
  updated_at
);

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

# Association
# User -> Comments

class User < ActiveRecord::Base
  has_many :comments
end

class Comment < ActiveRecord::Base
  belongs_to :user
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

Comment.delete_all

Comment.create(user_id: 1, body: "hello-1")
Comment.create(user_id: 1, body: "hello-2")
Comment.create(user_id: 2, body: "hello-3")



// ===== video_summary =====
関連するオブジェクトのデータをまとめて抽出する方法について見ていきます。


    関連データの抽出

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

# Association
# User -> Comments

class User < ActiveRecord::Base
  has_many :comments
end

class Comment < ActiveRecord::Base
  belongs_to :user
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

Comment.delete_all

Comment.create(user_id: 1, body: "hello-1")
Comment.create(user_id: 1, body: "hello-2")
Comment.create(user_id: 2, body: "hello-3")

# user = User.includes(:comments).find(1)
# # pp user.comments
# user.comments.each do |c|
#   puts "#{user.name}: #{c.body}"
# end

comments = Comment.includes(:user).all
comments.each do |c|
  puts "#{c.body} by #{c.user.name}"
end

// ===== import.sql =====
drop table if exists users;
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);

drop table if exists comments;
create table comments (
  id integer primary key,
  user_id integer,
  body text,
  created_at,
  updated_at
);



// ===== video_summary =====
関連するオブジェクトのデータもまとめて削除する方法について見ていきます。


    関連データの削除

// ===== main.rb =====
require 'active_record'
require 'pp'

Time.zone_default = Time.find_zone! 'Tokyo'
ActiveRecord::Base.default_timezone = :local

ActiveRecord::Base.establish_connection(
  "adapter" => "sqlite3",
  "database" => "./myapp.db"
)

# Association
# User -> Comments

class User < ActiveRecord::Base
  has_many :comments, dependent: :destroy
end

class Comment < ActiveRecord::Base
  belongs_to :user
end

User.delete_all

User.create(name: "tanaka", age: 19)
User.create(name: "takahashi", age: 25)
User.create(name: "hayashi", age: 31)
User.create(name: "mizutani", age: 28)
User.create(name: "otsuka", age: 35)

Comment.delete_all

Comment.create(user_id: 1, body: "hello-1")
Comment.create(user_id: 1, body: "hello-2")
Comment.create(user_id: 2, body: "hello-3")

User.find(1).destroy
pp Comment.select("id, user_id, body").all

// ===== import.sql =====
drop table if exists users;
create table users (
  id integer primary key,
  name text,
  age integer,
  created_at,
  updated_at
);

drop table if exists comments;
create table comments (
  id integer primary key,
  user_id integer,
  body text,
  created_at,
  updated_at
);



