// ===== video_summary =====
クラスの概要やインスタンスといった用語について解説していきます。


    クラスの概要
    インスタンス

// ===== main.php =====








// ===== video_summary =====
Userクラスにプロパティを設定してからインスタンスを作成していきます。


    Userクラス
    プロパティ
    new

// ===== commentBox 1 =====
補足情報
クラスの命名について




2024年12月4日





動画の0:22〜、「クラス名は最初を大文字にすることになっている」としていますが、仕様上は必ずしも大文字から始める必要はありません（ただ、慣習的に大文字で始めることがほとんどです）。訂正してお詫び申し上げます。

// ===== main.php =====
<?php
class User
{
    public $name;
    public $score;
}

$user1 = new User();
$user2 = new User();






// ===== video_summary =====
インスタンスのプロパティにアクセスする方法について見ていきます。


    プロパティへのアクセス
    動作確認

// ===== main.php =====
<?php
class User
{
    public $name;
    public $score;
}

$user1 = new User();
$user1->name = "Taro";
$user1->score = 70;
$user2 = new User();
$user2->name = "Jiro";
$user2->score = 90;

echo $user1->name . PHP_EOL;
echo $user1->score . PHP_EOL;
echo $user2->name . PHP_EOL;
echo $user2->score . PHP_EOL;





// ===== video_summary =====
newするときに必ず実行されるコンストラクタメソッドについて見ていきます。


    コンストラクタ
    メソッド
    $this

// ===== main.php =====
<?php
class User
{
    public $name;
    public $score;

    // public function __construct($nameFromNew, $scoreFromNew)
    // {
    //     $this->name = $nameFromNew;
    //     $this->score = $scoreFromNew;
    // }
    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }
}

$user1 = new User("Taro", 70);
// $user1->name = "Taro";
// $user1->score = 70;
$user2 = new User("Jiro", 90);
// $user2->name = "Jiro";
// $user2->score = 90;

echo $user1->name . PHP_EOL;
echo $user1->score . PHP_EOL;
echo $user2->name . PHP_EOL;
echo $user2->score . PHP_EOL;






// ===== video_summary =====
Userクラスに別のメソッドを定義する方法について見ていきます。


    getInfoメソッド
    メソッドの実行

// ===== main.php =====
<?php
class User
{
    public $name;
    public $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score}";
    }
}

$user1 = new User("Taro", 70);
$user2 = new User("Jiro", 90);

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
メソッドによってプロパティの値を設定する方法について見ていきます。


    値の変更
    setScoreメソッドの実装
    現状の問題点

// ===== main.php =====
<?php
class User
{
    public $name;
    public $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score}";
    }

    public function setScore($score)
    {
        if ($score < 0 || $score > 100) {
            echo "Invalid score!" . PHP_EOL;
            return;
        }
        $this->score = $score;
    }
}

$user1 = new User("Taro", 70);
$user2 = new User("Jiro", 90);

$user1->score = 900;
// $user1->setScore(900);

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;







// ===== video_summary =====
privateのアクセス修飾子をつけることによって、プロパティの値が変更されないようにしていきます。


    private
    アクセス修飾子
    カプセル化
    setter、getter

// ===== main.php =====
<?php
class User
{
    public $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score}";
    }

    // setter
    public function setScore($score)
    {
        if ($score < 0 || $score > 100) {
            echo "Invalid score!" . PHP_EOL;
            return;
        }
        $this->score = $score;
    }

    // getter
    public function getScore()
    {
        return $this->score;
    }
}

$user1 = new User("Taro", 70);
$user2 = new User("Jiro", 90);

// $user1->score = 900;
// $user1->setScore(900);
// echo $user1->score . PHP_EOL;
echo $user1->getScore() . PHP_EOL;

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
クラスの外からアクセスしてほしくないメソッドを設定する方法について見ていきます。


    getResultメソッド
    private

// ===== main.php =====
<?php
class User
{
    public $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score}, {$this->getResult()}";
    }

    private function getResult()
    {
        if ($this->score >= 80) {
            return "Pass";
        } else {
            return "Fail";
        }
    }
}

$user1 = new User("Taro", 70);
$user2 = new User("Jiro", 90);

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;
echo $user1->getResult() . PHP_EOL;
echo $user2->getResult() . PHP_EOL;






// ===== video_summary =====
インスタンスではなくて、クラス自体に紐づくプロパティを設定する方法について見ていきます。


    $count
    static
    インスタンスプロパティ
    クラスプロパティ

// ===== main.php =====
<?php
class User
{
    public $name;
    public $score;
    public static $count = 0;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
        User::$count++;
    }
}

// $count = 0;
$user1 = new User("Taro", 70);
// $count++;
$user2 = new User("Jiro", 90);
// $count++;

// echo $count . PHP_EOL;
echo User::$count . PHP_EOL;






// ===== video_summary =====
クラス自体に紐づくメソッドを定義する方法について見ていきます。


    アクセス修飾子の設定
    static
    インスタンスメソッド
    クラスメソッド

// ===== main.php =====
<?php
class User
{
    public $name;
    public $score;
    private static $count = 0;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
        User::$count++;
    }

    public static function getUserCount()
    {
        return User::$count;
    }
}

$user1 = new User("Taro", 70);
$user2 = new User("Jiro", 90);

// User::$count++;
// echo User::$count . PHP_EOL;
echo User::getUserCount() . PHP_EOL;






// ===== video_summary =====
Scoreクラスを作ってプログラムを拡張していきます。


    Scoreクラスの定義
    動作確認

// ===== main.php =====
<?php
class Score
{
    private $subject;
    private $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }
}

class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }
}

$user1 = new User("Taro", new Score("Math", 70));
$user2 = new User("Jiro", new Score("English", 90));






// ===== video_summary =====
ScoreクラスにgetInfoメソッドを実装していきます。


    getInfoメソッド
    クラスを分けるメリット

// ===== main.php =====
<?php
class Score
{
    private $subject;
    private $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}";
    }
}

class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        // return "{$this->name}, {$this->score->subject}";
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new Score("Math", 70));
$user2 = new User("Jiro", new Score("English", 90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;







// ===== video_summary =====
ScoreクラスにgetResultメソッドを実装していきます。


    getResultメソッド
    条件演算子

// ===== commentBox 1 =====
補足情報
条件演算子について




2024年6月2日





動画中で紹介している条件演算子については以下の動画を復習してみましょう。
・#12 条件演算子を使ってみよう | PHP入門 制御構造編
https://dotinstall.com/lessons/basic_php_controls/67112

// ===== main.php =====
<?php
class Score
{
    private $subject;
    private $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    private function getResult()
    {
        return $this->points >= 80 ? "Pass" : "Fail";
    }

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new Score("Math", 70));
$user2 = new User("Jiro", new Score("English", 90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
クラスの継承と、protectedについて図を使って説明していきます。


    クラスの継承
    親クラス/スーパークラス
    子クラス/サブクラス
    protected

// ===== main.php =====
<?php
class Score
{
    private $subject;
    private $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    private function getResult()
    {
        return $this->points >= 80 ? "Pass" : "Fail";
    }

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new Score("Math", 70));
$user2 = new User("Jiro", new Score("English", 90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
Scoreクラスを継承して、MathScoreクラス、EnglishScoreクラスを定義していきます。


    extends
    MathScore
    EnglishScore
    parent::__construct()

// ===== main.php =====
<?php
class Score
{
    private $subject;
    private $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    private function getResult()
    {
        return $this->points >= 80 ? "Pass" : "Fail";
    }

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }
}

class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }
}

class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new MathScore(70));
$user2 = new User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;





// ===== video_summary =====
子クラスで独自のメソッドを定義していく方法について見ていきます。


    独自ロジックの実装
    メソッドのオーバーライド

// ===== main.php =====
<?php
class Score
{
    private $subject;
    protected $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    protected function getResult()
    {
        return $this->points >= 80 ? "Pass" : "Fail";
    }

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }

    protected function getResult()
    {
        echo "MathScore method" . PHP_EOL;
        return $this->points >= 50 ? "Pass" : "Fail";
    }
}

class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }

    protected function getResult()
    {
        echo "EnglishScore method" . PHP_EOL;
        return $this->points >= 95 ? "Pass" : "Fail";
    }
}

class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new MathScore(70));
$user2 = new User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
子クラスで必ず実装してもらいたいメソッドを設定する方法について見ていきます。


    abstract
    抽象メソッド
    抽象クラス
    エラーの確認

// ===== main.php =====
<?php
abstract class Score
{
    private $subject;
    protected $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    abstract protected function getResult();

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }

    // protected function getResult()
    // {
    //     echo "MathScore method" . PHP_EOL;
    //     return $this->points >= 50 ? "Pass" : "Fail";
    // }
}

class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }

    protected function getResult()
    {
        echo "EnglishScore method" . PHP_EOL;
        return $this->points >= 95 ? "Pass" : "Fail";
    }
}

class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new MathScore(70));
$user2 = new User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;





// ===== video_summary =====
継承関係のないクラスで必ず実装してもらいたいメソッドを設定する方法について見ていきます。


    interface
    Loggableインターフェース
    implements
    エラーの確認

// ===== commentBox 1 =====
補足情報
インターフェースの命名について




2024年12月4日





動画の0:59〜、「インターフェースの最初の文字も大文字にする必要がある」としていますが、仕様上は必ずしも大文字から始める必要はありません（ただ、慣習的に大文字で始めることがほとんどです）。訂正してお詫び申し上げます。

// ===== main.php =====
<?php
interface Loggable
{
    public function log();
}

abstract class Score implements Loggable
{
    private $subject;
    protected $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    abstract protected function getResult();

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }

    protected function getResult()
    {
        echo "MathScore method" . PHP_EOL;
        return $this->points >= 50 ? "Pass" : "Fail";
    }
}

class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }

    protected function getResult()
    {
        echo "EnglishScore method" . PHP_EOL;
        return $this->points >= 95 ? "Pass" : "Fail";
    }
}

class User implements Loggable
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new MathScore(70));
$user2 = new User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
前回設定したlogメソッドを実装していきます。


    logメソッドの実装
    メソッドの実行

// ===== main.php =====
<?php
interface Loggable
{
    public function log();
}

abstract class Score implements Loggable
{
    private $subject;
    protected $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
        $this->log();
    }

    public function log()
    {
        echo "Instance created: {$this->subject}" . PHP_EOL;
    }

    abstract protected function getResult();

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }

    protected function getResult()
    {
        echo "MathScore method" . PHP_EOL;
        return $this->points >= 50 ? "Pass" : "Fail";
    }
}

class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }

    protected function getResult()
    {
        echo "EnglishScore method" . PHP_EOL;
        return $this->points >= 95 ? "Pass" : "Fail";
    }
}

class User implements Loggable
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
        $this->log();
    }

    public function log()
    {
        echo "Instance created: {$this->name}" . PHP_EOL;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new MathScore(70));
$user2 = new User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
任意のクラスにメソッドを組み込んでいくことができるトレイトについて見ていきます。


    trait
    use
    動作確認

// ===== main.php =====
<?php
trait LogTrait
{
    public function log()
    {
        echo "Instance created" . PHP_EOL;
    }
}

abstract class Score
{
    use LogTrait;

    private $subject;
    protected $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
        $this->log();
    }

    abstract protected function getResult();

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }

    protected function getResult()
    {
        echo "MathScore method" . PHP_EOL;
        return $this->points >= 50 ? "Pass" : "Fail";
    }
}

class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }

    protected function getResult()
    {
        echo "EnglishScore method" . PHP_EOL;
        return $this->points >= 95 ? "Pass" : "Fail";
    }
}

class User
{
    use LogTrait;

    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
        $this->log();
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

$user1 = new User("Taro", new MathScore(70));
$user2 = new User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
クラスを別々のファイルに分割していく方法について見ていきます。


    ファイルの分割
    require

// ===== EnglishScore.php =====
<?php
class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }

    protected function getResult()
    {
        return $this->points >= 95 ? "Pass" : "Fail";
    }
}

// ===== MathScore.php =====
<?php
class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }

    protected function getResult()
    {
        return $this->points >= 50 ? "Pass" : "Fail";
    }
}

// ===== Score.php =====
<?php
abstract class Score
{
    private $subject;
    protected $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    abstract protected function getResult();

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}

// ===== User.php =====
<?php
class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

// ===== main.php =====
<?php
require("Score.php");
require("MathScore.php");
require("EnglishScore.php");
require("User.php");

$user1 = new User("Taro", new MathScore(70));
$user2 = new User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;






// ===== video_summary =====
クラス名の衝突を避けるために使うことができる名前空間について見ていきます。


    namespace
    use

// ===== User.php =====
<?php
namespace Dotinstall\MyApp;

class User
{
    private $name;
    private $score;

    public function __construct($name, $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo()
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

// ===== main.php =====
<?php
require("Score.php");
require("MathScore.php");
require("EnglishScore.php");
require("User.php");

use Dotinstall\MyApp;

class User
{

}

// $user1 = new Dotinstall\MyApp\User("Taro", new MathScore(70));
// $user2 = new Dotinstall\MyApp\User("Jiro", new EnglishScore(90));
$user1 = new MyApp\User("Taro", new MathScore(70));
$user2 = new MyApp\User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;

// ===== EnglishScore.php =====
<?php
class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }

    protected function getResult()
    {
        return $this->points >= 95 ? "Pass" : "Fail";
    }
}

// ===== MathScore.php =====
<?php
class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }

    protected function getResult()
    {
        return $this->points >= 50 ? "Pass" : "Fail";
    }
}

// ===== Score.php =====
<?php
abstract class Score
{
    private $subject;
    protected $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    abstract protected function getResult();

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}





// ===== video_summary =====
クラスに型付けするための方法について見ていきます。


    declare(strict_types=1)
    クラスの型付け
    親クラスによる型付け

// ===== commentBox 1 =====
補足情報
関数の型付けについて




2024年6月2日





関数の型付けについて忘れてしまった人は以下の動画を復習してみましょう。
・#13 関数の型付けをしてみよう | PHP入門 関数編
https://dotinstall.com/lessons/basic_php_functions_v2/67213
・#14 union型を使ってみよう | PHP入門 関数編
https://dotinstall.com/lessons/basic_php_functions_v2/67214

// ===== User.php =====
<?php
class User
{
    private string $name;
    private Score $score;

    public function __construct(string $name, Score $score)
    {
        $this->name = $name;
        $this->score = $score;
    }

    public function getInfo(): string
    {
        return "{$this->name}, {$this->score->getInfo()}";
    }
}

// ===== main.php =====
<?php
declare(strict_types=1);

require("Score.php");
require("MathScore.php");
require("EnglishScore.php");
require("User.php");

$user1 = new User(100, new MathScore(70));
$user2 = new User("Jiro", new EnglishScore(90));

echo $user1->getInfo() . PHP_EOL;
echo $user2->getInfo() . PHP_EOL;

// ===== EnglishScore.php =====
<?php
class EnglishScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("English", $points);
    }

    protected function getResult()
    {
        return $this->points >= 95 ? "Pass" : "Fail";
    }
}

// ===== MathScore.php =====
<?php
class MathScore extends Score
{
    public function __construct($points)
    {
        parent::__construct("Math", $points);
    }

    protected function getResult()
    {
        return $this->points >= 50 ? "Pass" : "Fail";
    }
}

// ===== Score.php =====
<?php
abstract class Score
{
    private $subject;
    protected $points;

    public function __construct($subject, $points)
    {
        $this->subject = $subject;
        $this->points = $points;
    }

    abstract protected function getResult();

    public function getInfo()
    {
        return "{$this->subject}, {$this->points}, {$this->getResult()}";
    }
}








