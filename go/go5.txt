<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <pre>
 ポインタの概要について説明したのち、基本的な記法について確認していきます。

score := 70, メモリ上に領域を確保、70を格納、scoreの名前がつく
メモリ内の位置をあらわす番号、アドレス、0x300
アドレスを代入した変数、ポインタ、pScore
ポインタの基本的な記法
var pScore *int、変数宣言、ゼロ値はnil、アドレス未設定
pScore = &score、scoreのアドレスをpScoreに代入
*pScore、アドレスに格納された値にアクセス









ポインタの基本的な書き方と、値にアクセスする方法について見ていきます。

基本的な書き方とまとめて書く方法
var pScore *int、pScore = &score、基本的な書き方
pScore := &score、宣言から代入をまとめた書き方
scoreと*pScoreは70
&scoreとpScoreはアドレス
アドレスは16進数で表示、状況によって変わるので気にしない
*int、ポイントのデータ型を示す
*pScore、pScoreが参照している値にアクセスする

main.go
package main

import "fmt"

func main() {
    score := 70
    // var pScore *int
    // pScore = &score
    pScore := &score

    fmt.Println(score) // 70
    fmt.Println(&score) // address
    fmt.Println(pScore) // address
    fmt.Println(*pScore) // 70
}










活用方法を知るために、ポインタを使わないで1を足すプログラムを実装していきます。

活用方法を知るため、ポインタを使わないで実装
scoreに1を足してscoreに再代入して、表示する

main.go
package main

import "fmt"

func addScore(score int) int {
    score++
    return score
}

func main() {
    score := 70
    score = addScore(score)

    fmt.Println(score)







ポインタを使わない場合、使う場合での挙動について図で説明していきます。

説明図を使い、前回の動きとポインタを使ったときの動きを確認
ポインタを使わない場合、値をコピーして、仮引数に渡す
ポインタを使う場合、値をコピーせず、アドレスを保持するポインタを用意して渡す
ポインタを使うと効率的にメモリを使うことができる

main.go
package main

import "fmt"

func addScore(score int) int {
    score++
    return score
}

func main() {
    score := 70
    score = addScore(score)

    fmt.Println(score)
}






前々回のコードをポインタを使って書き換える方法について見ていきます。

ポインタを使って書き換える
scoreのアドレスが入ったpScoreを宣言し、addScore()にpScoreを渡す
func addScore(pScore *int)とし、値を直接操作、返り値を削除、scoreへの再代入は不要に
addScore()に直接アドレスを渡し、addScore(&score)とすることも可能

main.go
package main

import "fmt"

func addScore(pScore *int) {
    *pScore++
}

func main() {
    score := 70
    // pScore := &score
    // addScore(pScore)
    addScore(&score)

    fmt.Println(score)
}







ポインタと構造体の組み合わせについて見ていきます。

ポインタと構造体、配列、スライス、マップなど大きなデータとの組み合わせを確認していく
構造体のフィールドにアクセスする場合は、*不要、自動でデリファレンスされる
補足情報 構造体の記法について
2024年10月21日
動画内で紹介している構造体の記法について忘れてしまった人は以下の動画を復習してみましょう。

・#17 構造体の記法を確認しよう | Go言語入門 データ構造編
https://dotinstall.com/lessons/basic_golang_data_structure/68517

main.go
package main

import "fmt"

type student struct {
    name string
    subject string
    score int
}

func addScore(pStudent *student) {
    pStudent.score++
}

func main() {
    taro := student{
        name: "Taro",
        subject: "Math",
        score: 40,
    }
    addScore(&taro)

    fmt.Println(taro.score)







ポインタと配列の組み合わせについて見ていきます。

配列でポインタを使う
配列、スライス、マップのデータを用意、表示を確認
配列の中にアクセスする場合は、*不要、自動でデリファレンスされる、構造体と同様
補足情報 配列、スライス、マップの記法について
2024年10月21日
動画内で紹介している配列、スライス、マップの記法について忘れてしまった人は以下の動画を復習してみましょう。

・#01 配列の記法を確認しよう | Go言語入門 データ構造編
https://dotinstall.com/lessons/basic_golang_data_structure/68501
・#05 スライスを使ってみよう | Go言語入門 データ構造編
https://dotinstall.com/lessons/basic_golang_data_structure/68505
・#11 マップの記法を確認しよう | Go言語入門 データ構造編
https://dotinstall.com/lessons/basic_golang_data_structure/68511

main.go
package main

import "fmt"

func updateScoresArray(pScores *[3]int) {
    pScores[0] = 100
}

func main() {
    scoresArray := [3]int{10, 20, 30}
    scoresSlice := []int{40, 50, 60}
    scoresMap := map[string]int{
        "english": 70,
        "math":    80,
    }

    updateScoresArray(&scoresArray)

    fmt.Println("scoresArray:", scoresArray)
    fmt.Println("scoresSlice:", scoresSlice)
    fmt.Println("scoresMap:", scoresMap)
}







ポインタとスライス、マップの組み合わせを確認し、構造体、配列、スライス、マップの違いを整理していきます。

スライス、マップでポインタを使う
スライス、マップ自体がアドレスを保持しているポインタ
&不要、*不要、仮引数の*も不要
構造体、配列は値そのものを保持
スライス、マップは値のアドレスを保持
補足情報 スライスの挙動について
2024年10月21日
動画内で紹介しているスライスの挙動について忘れてしまった人は以下を復習してみましょう。

・#09 スライスの挙動を理解しよう | Go言語入門 データ構造編
https://dotinstall.com/lessons/basic_golang_data_structure/68509

main.go
package main

import "fmt"

func updateScoresArray(pScores *[3]int) {
    pScores[0] = 100
}

func updateScoresSlice(pScores []int) {
    pScores[0] = 100
}

func updateScoresMap(pScores map[string]int) {
    pScores["english"] = 100
}

func main() {
    scoresArray := [3]int{10, 20, 30}
    scoresSlice := []int{40, 50, 60}
    scoresMap := map[string]int{
        "english": 70,
        "math":    80,
    }

    updateScoresArray(&scoresArray)
    updateScoresSlice(scoresSlice)
    updateScoresMap(scoresMap)

    fmt.Println("scoresArray:", scoresArray)
    fmt.Println("scoresSlice:", scoresSlice)
    fmt.Println("scoresMap:", scoresMap)
}









デリファレンス時にエラーを防ぐためのnilチェックについて見ていきます。

ポインタを宣言、アドレス代入を忘れた際どうなるのか確認
nilが表示されるのではなく、エラーが表示される
デリファレンスの際にエラーが出ないように注意が必要
エラーにならないように、nilかどうかをチェックして条件分岐

main.go
package main

import "fmt"

func main() {
    score := 70
    var pScore *int

    fmt.Println(score)
    if pScore != nil {
        fmt.Println(*pScore)
    } else {
        fmt.Println("pScore is not initialized!")
    }
}








  </pre>
  
</body>
</html>